---
import Aurora from './Aurora.astro';
import HeroWordReveal from './HeroWordReveal.astro';
import CountUp from './CountUp.astro';
import MadeByDiggyPill from './MadeByDiggyPill.astro';
import { liveProducts, products } from '../data/products';
const liveCount = liveProducts.length;
const totalCount = products.length;
---

<section class="relative overflow-hidden">
  <Aurora intensity="high" />
  <div class="absolute inset-0 bg-grid opacity-60"></div>

  <div class="relative mx-auto max-w-7xl px-5 sm:px-8 pt-20 sm:pt-24 pb-24 sm:pb-32">
    <!-- Eyebrow row -->
    <div class="reveal reveal-1 flex flex-wrap items-center gap-3 mb-9">
      <div class="inline-flex items-center gap-2 rounded-full border border-[var(--color-line-strong)] bg-[var(--color-bg-card)] px-3.5 py-1.5">
        <span class="flex h-2 w-2 rounded-full bg-[#4ade80]" style="box-shadow: 0 0 12px #4ade80; animation: pulse 2s ease-in-out infinite"></span>
        <span class="text-[12.5px] text-[var(--color-muted)] font-mono">
          <span class="text-[var(--color-text)]">{liveCount}</span><span class="text-[var(--color-subtle)]">/{totalCount}</span> live
          <span class="mx-2 text-[var(--color-subtle)]">·</span>
          apps · v0.1.0
        </span>
      </div>
      <MadeByDiggyPill />
    </div>

    <!-- Headline with finite-rotate-then-lock -->
    <h1 class="reveal reveal-2 text-[44px] sm:text-[72px] md:text-[96px] lg:text-[112px] font-semibold tracking-[-0.04em] leading-[0.96] max-w-[14ch]">
      <span class="block gradient-text">Where gamers</span>
      <span class="block gradient-text">go to <HeroWordReveal lockWord="maxx." /></span>
    </h1>

    <!-- Subhead — three slaps, then the hook.
         Stamp tail in brand-shimmer (matches "None" in the stats grid + the
         brand-shimmer used on the product cards) instead of the Bowlby italic
         accent. Bowlby reads too chunky at subhead scale (22px); shimmer is
         the site's signature accent at this register. -->
    <p class="reveal reveal-3 mt-7 max-w-xl text-[19px] sm:text-[22px] leading-[1.4] text-[var(--color-text-mid)]">
      Free, native tools for competitive gamers and streamers — a PC tuner, a lighter Discord, an ad blocker, a stream studio, and more. <span class="font-mono text-[var(--color-subtle)] mx-1">·</span> <span class="font-semibold tracking-tight text-[var(--color-text)]">Built for the <span class="brand-shimmer">millisecond</span>.</span>
    </p>

    <!-- CTAs — primary now drives to the live tuner -->
    <div class="reveal reveal-4 mt-9 flex flex-wrap items-center gap-3">
      <a href="/optimizationmaxxing" class="btn-primary">
        Get the tuner
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M5 12h14M13 5l7 7-7 7"/>
        </svg>
      </a>
      <a href="#apps" class="btn-secondary">
        Browse the apps
      </a>
      <a href="/why" class="btn-secondary">
        Read our why
      </a>
    </div>

    <!-- Stats grid with animated counters — display font for the numbers, max impact. -->
    <div class="reveal reveal-5 mt-20 grid grid-cols-2 sm:grid-cols-4 gap-px rounded-2xl overflow-hidden border border-[var(--color-line)] bg-[var(--color-line)] max-w-3xl">
      <div class="bg-[var(--color-bg-card)] px-5 py-6">
        <p class="text-[11px] uppercase tracking-[0.08em] text-[var(--color-subtle)] font-mono">Tweaks shipped</p>
        <p class="font-display mt-2 text-[48px] sm:text-[56px] gradient-suite"><CountUp value={100} /></p>
      </div>
      <div class="bg-[var(--color-bg-card)] px-5 py-6">
        <p class="text-[11px] uppercase tracking-[0.08em] text-[var(--color-subtle)] font-mono">Discord RAM cut</p>
        <p class="font-display mt-2 text-[48px] sm:text-[56px] gradient-suite"><CountUp value={72} suffix="%" /></p>
      </div>
      <div class="bg-[var(--color-bg-card)] px-5 py-6">
        <p class="text-[11px] uppercase tracking-[0.08em] text-[var(--color-subtle)] font-mono">Tuner installer</p>
        <p class="font-display mt-2 text-[48px] sm:text-[56px] gradient-suite"><CountUp value={3.6} /> <span class="text-[16px] text-[var(--color-muted)]">MB</span></p>
      </div>
      <div class="bg-[var(--color-bg-card)] px-5 py-6">
        <p class="text-[11px] uppercase tracking-[0.08em] text-[var(--color-subtle)] font-mono">Subscriptions</p>
        <p class="font-display mt-2 text-[48px] sm:text-[56px]">
          <span class="brand-shimmer">None</span>
        </p>
      </div>
    </div>
  </div>
</section>
