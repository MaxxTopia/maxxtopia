---
import Layout from '../../layouts/Layout.astro';

const editorUrl = 'https://montage.maxxtopia.com/';
const showcaseVideo = '/screenshots/montagemaxxer/peterbot-showcase.mp4';
const showcasePoster = '/screenshots/montagemaxxer/peterbot-showcase-poster.webp';
const pageTitle = 'MontageMaxxer — Build Your Montage, Your Way';
const pageDescription =
  'A local-first montage editor for Fortnite, Valorant, and serious gameplay edits. Tune titles, songs, clip volume, pacing, and the final render in one place.';

const softwareSchema = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name: 'MontageMaxxer',
  alternateName: 'montagemaxxer',
  description: pageDescription,
  applicationCategory: 'MultimediaApplication',
  applicationSubCategory: 'Video editing',
  operatingSystem: 'Windows',
  offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
  publisher: { '@type': 'Organization', name: 'Maxxtopia', url: 'https://maxxtopia.com' },
  url: 'https://maxxtopia.com/montagemaxxer/',
};

const breadcrumbSchema = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://maxxtopia.com/' },
    { '@type': 'ListItem', position: 2, name: 'MontageMaxxer', item: 'https://maxxtopia.com/montagemaxxer/' },
  ],
};
---

<Layout title={pageTitle} description={pageDescription}>
  <script type="application/ld+json" set:html={JSON.stringify(softwareSchema)} />
  <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} />

  <div class="mmx-page">
    <section class="mmx-hero">
      <div class="mmx-noise" aria-hidden="true"></div>
      <div class="mmx-orbit mmx-orbit--one" aria-hidden="true"></div>
      <div class="mmx-orbit mmx-orbit--two" aria-hidden="true"></div>

      <div class="mmx-wrap mmx-hero-grid">
        <div class="mmx-copy">
          <a href="/#apps" class="mmx-back">&larr; All products</a>
          <p class="mmx-eyebrow"><span class="mmx-live-dot"></span> Local-first montage editor &middot; Fortnite + Valorant</p>
          <h1>Your montage.<br /><em>Your timing.</em></h1>
          <p class="mmx-lead">
            Build the cut you actually hear in your head. Tune the title, the outro,
            the song, the clip mix, and the pacing — then render it on your own machine.
          </p>
          <div class="mmx-actions">
            <a href={editorUrl} class="mmx-button mmx-button--primary" target="_blank" rel="noopener noreferrer">
              Open MontageMaxxer
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M7 17 17 7M8 7h9v9" /></svg>
            </a>
            <a href="#controls" class="mmx-button mmx-button--ghost">See the controls</a>
          </div>
          <p class="mmx-note">Private editor host &middot; render stays with the machine that has your clips</p>
        </div>

        <div class="mmx-stage mmx-showcase" aria-label="A real MontageMaxxer export showcase">
          <div class="mmx-stage-glow" aria-hidden="true"></div>
          <div class="mmx-showcase-kicker" aria-hidden="true"><span class="mmx-live-dot"></span><b>REAL EXPORT / PLAYBACK</b><i></i><span>01 / 01</span></div>

          <figure class="mmx-showcase-window">
            <div class="mmx-window-bar">
              <span class="mmx-window-brand"><i></i> MONTAGEMAXXER</span>
              <span class="mmx-window-mode">FINAL CUT / PROOF</span>
              <span class="mmx-window-dots" aria-hidden="true"><b></b><b></b><b></b></span>
            </div>
            <div class="mmx-showcase-media">
              <video class="mmx-showcase-video" poster={showcasePoster} autoplay muted loop playsinline controls preload="metadata" aria-label="A real Fortnite montage rendered with MontageMaxxer">
                <source src={showcaseVideo} type="video/mp4" />
              </video>
              <div class="mmx-media-overlay mmx-media-overlay--top" aria-hidden="true"><span>REAL EXPORT</span><span>CLICK SOUND ON</span></div>
              <div class="mmx-media-overlay mmx-media-overlay--bottom" aria-hidden="true"><span>PETERBOT / FINAL PLAY</span><span>MADE ON LOCAL MACHINE</span></div>
            </div>
            <figcaption class="mmx-showcase-caption">
              <div><span class="mmx-showcase-label">MADE WITH MONTAGEMAXXER</span><strong>PETERBOT / FINAL PLAY</strong></div>
              <div class="mmx-showcase-spec"><b>1080P</b><span>60 FPS</span></div>
            </figcaption>
          </figure>

          <div class="mmx-showcase-float mmx-showcase-float--left" aria-hidden="true"><span>THE INPUT</span><b>YOUR FOOTAGE</b><i>stays local</i></div>
          <div class="mmx-showcase-float mmx-showcase-float--right" aria-hidden="true"><span>THE RESULT</span><b>REAL FINAL CUT</b><i>title + mix + outro</i></div>
          <div class="mmx-showcase-trace" aria-hidden="true"><span>01 SOURCE</span><i></i><span>02 EDIT</span><i></i><b>03 EXPORT</b></div>
          <p class="mmx-stage-a11y">This is a real Fortnite montage rendered with MontageMaxxer. It autoplays muted; use the video controls to turn sound on.</p>
        </div>
      </div>
    </section>

    <section class="mmx-receipts" aria-label="MontageMaxxer highlights">
      <div class="mmx-wrap mmx-receipts-grid">
        <div><span class="mmx-receipt-value">01</span><span class="mmx-receipt-label">drag the title</span></div>
        <div><span class="mmx-receipt-value">02</span><span class="mmx-receipt-label">hear the exact song</span></div>
        <div><span class="mmx-receipt-value">03</span><span class="mmx-receipt-label">render the final cut</span></div>
      </div>
    </section>

    <section class="mmx-section" id="controls">
      <div class="mmx-wrap">
        <p class="mmx-tag">01 &middot; The controls</p>
        <div class="mmx-section-heading">
          <h2>Every part that changes<br /><em>the feel of a montage.</em></h2>
          <p>Good pacing is not a preset you accept blindly. It is a series of small decisions you can see, hear, and change before the render costs you time.</p>
        </div>

        <div class="mmx-control-grid">
          <article class="mmx-control-card mmx-control-card--paper">
            <div class="mmx-card-number">01</div>
            <div class="mmx-card-icon">T</div>
            <h3>Titles that land where you put them.</h3>
            <p>Open the editor, drag intro or outro text in the preview, zoom the canvas, then save the style by name. Font, size, color, outline, shadow, and motion stay editable.</p>
            <div class="mmx-card-foot"><span>INTRO + OUTRO</span><b>drag / save / reuse</b></div>
          </article>
          <article class="mmx-control-card mmx-control-card--song">
            <div class="mmx-card-number">02</div>
            <div class="mmx-card-icon">♫</div>
            <h3>Song identity that does not drift.</h3>
            <p>Preview a real track before you commit. The selected song carries through beat mapping and render, so a near-match cannot quietly replace the song you chose.</p>
            <div class="mmx-card-foot"><span>PREVIEW</span><b>exact pick / exact render</b></div>
          </article>
          <article class="mmx-control-card mmx-control-card--mix">
            <div class="mmx-card-number">03</div>
            <div class="mmx-card-icon">↗</div>
            <h3>Shape the mix in groups.</h3>
            <p>Ctrl-click the clips that need the same treatment and raise or lower them together. Fine-tune one clip when it deserves its own move.</p>
            <div class="mmx-card-foot"><span>MULTI-SELECT</span><b>one change / many clips</b></div>
          </article>
        </div>
      </div>
    </section>

    <section class="mmx-section mmx-section--dark">
      <div class="mmx-wrap mmx-build-grid">
        <div>
          <p class="mmx-tag">02 &middot; The workflow</p>
          <h2>From raw clips<br /><em>to your signature.</em></h2>
          <p class="mmx-body">Pick a lane that fits the footage, keep the song honest, and make the last frame feel intentional. The editor gives you the decisions without burying them in a wall of controls.</p>
          <a href={editorUrl} class="mmx-inline-link" target="_blank" rel="noopener noreferrer">Open the editor <span>&rarr;</span></a>
        </div>
        <ol class="mmx-steps">
          <li><span>01</span><div><b>Load your clips</b><p>See the whole timeline before committing to the order.</p></div></li>
          <li><span>02</span><div><b>Choose the lane</b><p>Match look and pacing to the game, footage, and song.</p></div></li>
          <li><span>03</span><div><b>Make the authored moves</b><p>Titles, outro, group volume, sound design, and optional branding.</p></div></li>
          <li><span>04</span><div><b>Render with confidence</b><p>Review the exact song and final settings before export.</p></div></li>
        </ol>
      </div>
    </section>

    <section class="mmx-final">
      <div class="mmx-wrap mmx-final-inner">
        <p class="mmx-tag">03 &middot; Your cut</p>
        <h2>Make the edit feel<br /><em>like you.</em></h2>
        <p>MontageMaxxer is for the player who wants the final say — without rebuilding the same title, mix, and outro every time.</p>
        <div class="mmx-actions">
          <a href={editorUrl} class="mmx-button mmx-button--primary" target="_blank" rel="noopener noreferrer">Open MontageMaxxer <span>&rarr;</span></a>
          <a href="/clipmaxxer/apply" class="mmx-button mmx-button--ghost">Want it done for you?</a>
        </div>
      </div>
    </section>
  </div>
</Layout>

<style>
  .mmx-page { --mmx-ink: #f6f1e8; --mmx-muted: #a7a1b2; --mmx-paper: #f2c45b; --mmx-cyan: #27d9c3; --mmx-red: #db5948; --mmx-line: rgba(255,255,255,.12); background: #08090e; color: var(--mmx-ink); overflow: hidden; }
  .mmx-wrap { width: min(1180px, calc(100% - 40px)); margin: 0 auto; }
  .mmx-hero { position: relative; min-height: min(850px, calc(100vh - 56px)); display: grid; align-items: center; isolation: isolate; background: radial-gradient(circle at 82% 48%, rgba(39,217,195,.12), transparent 24%), radial-gradient(circle at 17% 18%, rgba(219,89,72,.13), transparent 26%), #08090e; }
  .mmx-hero::after { content: ''; position: absolute; inset: 0; z-index: -1; opacity: .23; pointer-events: none; background-image: linear-gradient(rgba(255,255,255,.035) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,.035) 1px, transparent 1px); background-size: 46px 46px; mask-image: linear-gradient(90deg, black, transparent 92%); }
  .mmx-noise { position: absolute; inset: 0; z-index: -1; opacity: .07; pointer-events: none; background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 180 180' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.8' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.55'/%3E%3C/svg%3E"); mix-blend-mode: screen; }
  .mmx-orbit { position: absolute; z-index: -1; border: 1px solid rgba(39,217,195,.18); border-radius: 50%; transform: rotate(-25deg); pointer-events: none; }
  .mmx-orbit--one { width: 670px; height: 300px; right: -180px; top: 18%; box-shadow: 0 0 80px rgba(39,217,195,.08); }
  .mmx-orbit--two { width: 520px; height: 220px; right: -70px; top: 29%; border-color: rgba(242,196,91,.18); }
  .mmx-hero-grid { display: grid; grid-template-columns: minmax(0, .92fr) minmax(480px, 1.08fr); align-items: center; gap: clamp(32px, 6vw, 90px); padding: 78px 0 88px; }
  .mmx-back { display: inline-flex; margin-bottom: 34px; color: var(--mmx-muted); font: 12px var(--font-mono); letter-spacing: .07em; text-decoration: none; transition: color .2s ease; }
  .mmx-back:hover { color: var(--mmx-cyan); }
  .mmx-eyebrow, .mmx-tag { color: var(--mmx-cyan); font: 11px var(--font-mono); letter-spacing: .14em; text-transform: uppercase; }
  .mmx-eyebrow { display: flex; align-items: center; gap: 9px; margin: 0 0 20px; }
  .mmx-live-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--mmx-cyan); box-shadow: 0 0 15px var(--mmx-cyan); }
  .mmx-copy h1, .mmx-section h2, .mmx-final h2 { margin: 0; font: 400 clamp(66px, 8.2vw, 118px)/.85 var(--font-display); letter-spacing: .012em; text-transform: uppercase; }
  .mmx-copy h1 em, .mmx-section h2 em, .mmx-final h2 em { color: var(--mmx-paper); font-style: normal; }
  .mmx-lead { max-width: 32rem; margin: 30px 0 0; color: #cec7d6; font-size: clamp(17px, 1.8vw, 20px); line-height: 1.55; }
  .mmx-actions { display: flex; flex-wrap: wrap; gap: 12px; align-items: center; margin-top: 30px; }
  .mmx-button { display: inline-flex; align-items: center; justify-content: center; gap: 10px; min-height: 50px; padding: 0 20px; border-radius: 999px; font: 600 13px var(--font-mono); text-decoration: none; transition: transform .2s ease, border-color .2s ease, background .2s ease, box-shadow .2s ease; }
  .mmx-button:hover { transform: translateY(-2px); }
  .mmx-button--primary { color: #081313; background: var(--mmx-cyan); box-shadow: 0 12px 35px rgba(39,217,195,.24); }
  .mmx-button--primary:hover { box-shadow: 0 15px 45px rgba(39,217,195,.38); }
  .mmx-button--ghost { color: var(--mmx-ink); border: 1px solid var(--mmx-line); background: rgba(255,255,255,.03); }
  .mmx-button--ghost:hover { border-color: rgba(242,196,91,.55); background: rgba(242,196,91,.07); }
  .mmx-note { margin: 18px 0 0; color: #827b8b; font: 11px var(--font-mono); letter-spacing: .03em; }

  .mmx-stage { --mmx-loop-duration: 8.8s; --mmx-drift-duration: 7s; --mmx-wave-duration: 1.1s; position: relative; min-height: 640px; display: grid; place-items: center; perspective: 1400px; }
  .mmx-stage-glow { position: absolute; width: 80%; height: 70%; border-radius: 50%; background: rgba(39,217,195,.12); filter: blur(70px); animation: mmx-glow-breathe var(--mmx-drift-duration) ease-in-out infinite; }
  @keyframes mmx-glow-breathe { 0%,100% { opacity: .55; transform: scale(.92); } 50% { opacity: 1; transform: scale(1.08); } }
  .mmx-stage-kicker { position: absolute; left: 1%; top: 3%; z-index: 5; display: flex; align-items: center; gap: 9px; color: rgba(242,238,231,.6); font: 9px var(--font-mono); letter-spacing: .14em; }
  .mmx-stage-kicker b { color: #d5d9e0; font-weight: 400; }
  .mmx-stage-kicker > i { width: 18px; height: 1px; background: rgba(255,255,255,.2); }
  .mmx-stage-kicker > span:last-child { color: var(--mmx-paper); }
  .mmx-stage-kicker .mmx-live-dot { width: 7px; height: 7px; flex: 0 0 auto; animation: mmx-signal-pulse 1.8s ease-in-out infinite; }
  @keyframes mmx-signal-pulse { 0%,100% { opacity: .52; } 50% { opacity: 1; } }
  .mmx-stage-state { position: relative; display: inline-grid; min-width: 78px; height: 11px; color: var(--mmx-paper); }
  .mmx-stage-state__item { grid-area: 1 / 1; color: inherit; font-style: normal; opacity: 0; white-space: nowrap; }
  .mmx-stage-state__item--source { animation: mmx-stage-state-source var(--mmx-loop-duration) linear infinite; }
  .mmx-stage-state__item--cut { animation: mmx-stage-state-cut var(--mmx-loop-duration) linear infinite; }
  .mmx-stage-state__item--render { animation: mmx-stage-state-render var(--mmx-loop-duration) linear infinite; }
  @keyframes mmx-stage-state-source { 0%,20% { opacity: 1; } 28%,100% { opacity: 0; } }
  @keyframes mmx-stage-state-cut { 0%,24% { opacity: 0; } 32%,62% { opacity: 1; } 70%,100% { opacity: 0; } }
  @keyframes mmx-stage-state-render { 0%,64% { opacity: 0; } 72%,94% { opacity: 1; } 100% { opacity: 0; } }
  .mmx-source-stack { position: absolute; left: -3%; bottom: 8%; z-index: 1; width: 152px; height: 106px; transform: rotate(-8deg); }
  .mmx-source-card { position: absolute; display: grid; gap: 4px; width: 138px; padding: 12px 13px; border: 1px solid rgba(255,255,255,.16); border-radius: 7px; background: #11151d; box-shadow: 0 18px 34px rgba(0,0,0,.38); font-family: var(--font-mono); }
  .mmx-source-card span { color: #6f7886; font-size: 8px; letter-spacing: .12em; }
  .mmx-source-card b { color: #edf0f2; font-size: 11px; font-weight: 500; }
  .mmx-source-card i { color: var(--mmx-cyan); font-size: 9px; font-style: normal; }
  .mmx-source-card--raw { left: 0; top: 18px; transform: rotate(-4deg); animation: mmx-source-float var(--mmx-drift-duration) ease-in-out infinite; }
  .mmx-source-card--selected { left: 17px; top: 0; z-index: 1; border-color: rgba(242,196,91,.34); transform: rotate(3deg); animation: mmx-source-selected var(--mmx-loop-duration) ease-in-out infinite; }
  @keyframes mmx-source-float { 0%,100% { transform: translateY(0) rotate(-4deg); } 50% { transform: translateY(-6px) rotate(-2deg); } }
  @keyframes mmx-source-selected { 0%,14%,100% { transform: translateY(0) rotate(3deg); box-shadow: 0 18px 34px rgba(0,0,0,.38); } 24%,58% { transform: translateY(-8px) rotate(1deg); box-shadow: 0 24px 42px rgba(0,0,0,.48), 0 0 22px rgba(242,196,91,.12); } 70% { transform: translateY(0) rotate(3deg); } }
  .mmx-window { position: relative; z-index: 2; width: min(630px, 100%); padding: 10px; border: 1px solid rgba(255,255,255,.17); border-radius: 16px; background: linear-gradient(145deg, #1b1e25, #0c0e13 55%); box-shadow: 0 40px 100px rgba(0,0,0,.65), 0 0 0 1px rgba(255,255,255,.04) inset, 0 0 80px rgba(39,217,195,.13); transform-origin: 50% 54%; animation: mmx-window-drift var(--mmx-drift-duration) ease-in-out infinite; }
  @keyframes mmx-window-drift { 0%,100% { transform: translate3d(0,0,0) rotate(1.8deg); } 50% { transform: translate3d(0,-7px,0) rotate(1.1deg); } }
  .mmx-window-bar { display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; height: 33px; padding: 0 8px 0 5px; color: #a6acb8; font: 9px var(--font-mono); letter-spacing: .08em; }
  .mmx-window-brand { display: flex; gap: 7px; align-items: center; color: #e5e7eb; }
  .mmx-window-brand i { display: block; width: 8px; height: 8px; border-radius: 2px; background: var(--mmx-cyan); box-shadow: 0 0 9px var(--mmx-cyan); animation: mmx-signal-pulse 1.8s ease-in-out infinite; }
  .mmx-window-mode { color: #7b8491; }
  .mmx-window-dots { display: flex; justify-content: flex-end; gap: 5px; }
  .mmx-window-dots b { width: 6px; height: 6px; border: 1px solid #737987; border-radius: 50%; }
  .mmx-preview { position: relative; aspect-ratio: 16 / 9; overflow: hidden; background: linear-gradient(145deg, #20283b, #0d1019 52%, #291b24); }
  .mmx-preview::before { content: ''; position: absolute; inset: 0; background: radial-gradient(circle at 72% 37%, rgba(39,217,195,.32), transparent 18%), linear-gradient(115deg, transparent 46%, rgba(255,255,255,.1) 46.5%, transparent 48%); animation: mmx-preview-breathe var(--mmx-drift-duration) ease-in-out infinite; }
  .mmx-preview::after { content: ''; position: absolute; inset: 0; pointer-events: none; background: linear-gradient(110deg, transparent 0 45%, rgba(255,255,255,.08) 47%, transparent 49% 100%); opacity: .56; }
  @keyframes mmx-preview-breathe { 0%,100% { opacity: .7; transform: scale(1); } 50% { opacity: 1; transform: scale(1.04); } }
  .mmx-preview-grid { position: absolute; inset: 0; opacity: .3; background-image: linear-gradient(rgba(255,255,255,.08) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,.08) 1px, transparent 1px); background-size: 30px 30px; mask-image: linear-gradient(140deg, black 8%, transparent 74%); }
  .mmx-preview-scan { position: absolute; left: -10%; right: -10%; top: -2px; z-index: 4; height: 2px; background: linear-gradient(90deg, transparent, var(--mmx-cyan), transparent); box-shadow: 0 0 16px var(--mmx-cyan); opacity: .65; animation: mmx-preview-scan var(--mmx-loop-duration) linear infinite; }
  @keyframes mmx-preview-scan { 0% { top: -2px; opacity: 0; } 10%,82% { opacity: .65; } 92%,100% { top: calc(100% + 2px); opacity: 0; } }
  .mmx-preview-kicker { position: absolute; left: 7%; top: 9%; z-index: 2; color: #a5f6eb; font: 9px var(--font-mono); letter-spacing: .13em; }
  .mmx-preview-signal { position: absolute; right: 6%; bottom: 8%; z-index: 2; display: flex; align-items: center; gap: 6px; color: rgba(247,242,232,.62); font: 8px var(--font-mono); letter-spacing: .1em; }
  .mmx-preview-signal span, .mmx-preview-cut span { display: inline-block; width: 5px; height: 5px; border-radius: 50%; background: var(--mmx-cyan); box-shadow: 0 0 10px var(--mmx-cyan); }
  .mmx-preview-title { position: absolute; left: 7%; top: 25%; z-index: 2; color: #f7f2e8; font: 400 clamp(48px, 8vw, 86px)/.78 var(--font-display); letter-spacing: .025em; text-shadow: 5px 6px 0 rgba(219,89,72,.85); }
  .mmx-preview-title span { color: var(--mmx-paper); }
  .mmx-preview-title--a { animation: mmx-title-card-a var(--mmx-loop-duration) ease-in-out infinite; }
  .mmx-preview-title--b { opacity: 0; transform: translateY(8px) scale(.97); animation: mmx-title-card-b var(--mmx-loop-duration) ease-in-out infinite; }
  @keyframes mmx-title-card-a { 0%,8% { opacity: 1; transform: translateY(0) scale(1); } 16%,43% { opacity: 1; transform: translateY(0) scale(1); } 51%,100% { opacity: 0; transform: translateY(-8px) scale(1.03); } }
  @keyframes mmx-title-card-b { 0%,48% { opacity: 0; transform: translateY(8px) scale(.97); } 57%,84% { opacity: 1; transform: translateY(0) scale(1); } 93%,100% { opacity: 0; transform: translateY(-8px) scale(1.03); } }
  .mmx-paper-strip { position: absolute; right: 6%; bottom: 18%; z-index: 2; padding: 9px 13px 8px; background: #f2eee4; color: #12141a; transform: rotate(-5deg); box-shadow: 5px 6px 0 rgba(219,89,72,.8); animation: mmx-paper-lift var(--mmx-drift-duration) ease-in-out infinite; }
  .mmx-paper-strip span { font: 10px var(--font-mono); letter-spacing: .09em; }
  @keyframes mmx-paper-lift { 0%,100% { transform: translateY(0) rotate(-5deg); } 50% { transform: translateY(-5px) rotate(-3deg); } }
  .mmx-preview-cut { position: absolute; left: 7%; bottom: 9%; z-index: 2; display: flex; align-items: center; gap: 6px; color: var(--mmx-paper); font: 8px var(--font-mono); letter-spacing: .08em; animation: mmx-cut-breathe var(--mmx-loop-duration) ease-in-out infinite; }
  @keyframes mmx-cut-breathe { 0%,38%,100% { opacity: .3; } 45%,65% { opacity: 1; } 72% { opacity: .42; } }
  .mmx-preview-hud { position: absolute; right: 5%; top: 9%; z-index: 2; display: flex; gap: 10px; color: rgba(247,242,232,.72); font: 9px var(--font-mono); }
  .mmx-timecode { position: relative; display: inline-grid; min-width: 43px; height: 10px; }
  .mmx-timecode__item { grid-area: 1 / 1; font-style: normal; opacity: 0; white-space: nowrap; }
  .mmx-timecode__item--source { animation: mmx-timecode-source var(--mmx-loop-duration) linear infinite; }
  .mmx-timecode__item--cut { animation: mmx-timecode-cut var(--mmx-loop-duration) linear infinite; }
  .mmx-timecode__item--render { animation: mmx-timecode-render var(--mmx-loop-duration) linear infinite; }
  @keyframes mmx-timecode-source { 0%,20% { opacity: 1; } 28%,100% { opacity: 0; } }
  @keyframes mmx-timecode-cut { 0%,24% { opacity: 0; } 32%,62% { opacity: 1; } 70%,100% { opacity: 0; } }
  @keyframes mmx-timecode-render { 0%,64% { opacity: 0; } 72%,94% { opacity: 1; } 100% { opacity: 0; } }
  .mmx-timeline { position: relative; padding: 10px 10px 8px; background: #11141b; border-top: 1px solid rgba(255,255,255,.08); }
  .mmx-timeline-head { display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-bottom: 8px; color: #7d8795; font: 8px var(--font-mono); letter-spacing: .12em; }
  .mmx-phase-rail { display: flex; align-items: center; justify-content: flex-end; gap: 4px; min-width: 0; }
  .mmx-phase { display: inline-flex; align-items: center; gap: 4px; padding: 3px 5px; border: 1px solid rgba(255,255,255,.1); border-radius: 999px; color: #697480; white-space: nowrap; animation-duration: var(--mmx-loop-duration); animation-timing-function: linear; animation-iteration-count: infinite; }
  .mmx-phase strong { color: #59646f; font-size: 7px; font-weight: 400; letter-spacing: .03em; }
  .mmx-phase b { color: inherit; font-size: 7px; font-weight: 400; letter-spacing: .07em; }
  .mmx-phase--source { animation-name: mmx-phase-source; }
  .mmx-phase--cut { animation-name: mmx-phase-cut; }
  .mmx-phase--render { animation-name: mmx-phase-render; }
  .mmx-phase-arrow { color: #4e5965; font-style: normal; font-size: 9px; }
  @keyframes mmx-phase-source { 0%,20% { color: var(--mmx-paper); border-color: rgba(242,196,91,.55); background: rgba(242,196,91,.09); box-shadow: 0 0 13px rgba(242,196,91,.12); } 28%,100% { color: #697480; border-color: rgba(255,255,255,.1); background: transparent; box-shadow: none; } }
  @keyframes mmx-phase-cut { 0%,24% { color: #697480; border-color: rgba(255,255,255,.1); background: transparent; box-shadow: none; } 32%,62% { color: var(--mmx-cyan); border-color: rgba(39,217,195,.55); background: rgba(39,217,195,.08); box-shadow: 0 0 13px rgba(39,217,195,.12); } 70%,100% { color: #697480; border-color: rgba(255,255,255,.1); background: transparent; box-shadow: none; } }
  @keyframes mmx-phase-render { 0%,64% { color: #697480; border-color: rgba(255,255,255,.1); background: transparent; box-shadow: none; } 72%,94% { color: var(--mmx-paper); border-color: rgba(242,196,91,.55); background: rgba(242,196,91,.09); box-shadow: 0 0 13px rgba(242,196,91,.12); } 100% { color: #697480; border-color: rgba(255,255,255,.1); background: transparent; box-shadow: none; } }
  .mmx-timeline-ruler { display: flex; justify-content: space-between; padding-left: 43px; color: #5e6875; font: 7px var(--font-mono); }
  .mmx-track { position: relative; display: grid; grid-template-columns: 35px minmax(0, 1fr); gap: 8px; align-items: center; height: 23px; }
  .mmx-track-label { color: #65707d; font: 7px var(--font-mono); letter-spacing: .08em; }
  .mmx-track-lane { position: relative; display: flex; gap: 3px; min-width: 0; height: 13px; overflow: hidden; border: 1px solid rgba(255,255,255,.07); border-radius: 2px; background: #0b0e14; }
  .mmx-timeline-clip { display: block; flex: 0 0 auto; height: 100%; border-radius: 1px; background: linear-gradient(90deg, #277f89, var(--mmx-cyan)); opacity: .52; animation: mmx-clip-focus var(--mmx-loop-duration) ease-in-out infinite; animation-delay: var(--mmx-delay, 0s); }
  .mmx-timeline-clip--one { width: 15%; } .mmx-timeline-clip--two { width: 11%; background: linear-gradient(90deg, #9b7638, var(--mmx-paper)); } .mmx-timeline-clip--three { width: 18%; } .mmx-timeline-clip--four { width: 10%; background: linear-gradient(90deg, #9b7638, var(--mmx-paper)); } .mmx-timeline-clip--five { width: 19%; } .mmx-timeline-clip--six { width: 17%; background: linear-gradient(90deg, #9b7638, var(--mmx-paper)); }
  @keyframes mmx-clip-focus { 0%,100% { opacity: .34; filter: saturate(.75); } 8%,25% { opacity: .9; filter: saturate(1.15); } 34% { opacity: .45; filter: saturate(.85); } }
  .mmx-kill-marker { position: absolute; top: -2px; bottom: -2px; z-index: 2; width: 2px; background: var(--mmx-red); box-shadow: 0 0 8px rgba(219,89,72,.75); transform: skew(-12deg); animation: mmx-marker-pulse var(--mmx-loop-duration) ease-in-out infinite; }
  .mmx-kill-marker--one { left: 29%; } .mmx-kill-marker--two { left: 56%; animation-delay: -2.6s; } .mmx-kill-marker--three { left: 79%; animation-delay: -5.1s; }
  @keyframes mmx-marker-pulse { 0%,100% { opacity: .34; } 12%,28% { opacity: 1; } 38% { opacity: .42; } }
  .mmx-title-block { display: block; height: 100%; border-radius: 1px; background: var(--mmx-paper); opacity: .7; }
  .mmx-title-block--intro { width: 22%; margin-left: 4%; } .mmx-title-block--outro { width: 14%; margin-left: auto; margin-right: 5%; background: var(--mmx-red); }
  .mmx-mini-wave { display: flex; align-items: center; justify-content: space-around; width: 100%; height: 100%; padding: 0 3px; }
  .mmx-mini-wave i { width: 2px; height: var(--mmx-wave-height, 45%); border-radius: 2px; background: var(--mmx-cyan); opacity: .66; transform-origin: center; animation: mmx-wave-dance var(--mmx-wave-duration) ease-in-out infinite alternate; animation-delay: calc(var(--mmx-wave-index, 0) * -0.08s); }
  .mmx-mini-wave i:nth-child(2n) { --mmx-wave-height: 75%; } .mmx-mini-wave i:nth-child(3n) { --mmx-wave-height: 35%; } .mmx-mini-wave i:nth-child(5n) { --mmx-wave-height: 92%; background: var(--mmx-paper); } .mmx-mini-wave i:nth-child(1) { --mmx-wave-index: 1; } .mmx-mini-wave i:nth-child(2) { --mmx-wave-index: 2; } .mmx-mini-wave i:nth-child(3) { --mmx-wave-index: 3; } .mmx-mini-wave i:nth-child(4) { --mmx-wave-index: 4; } .mmx-mini-wave i:nth-child(5) { --mmx-wave-index: 5; } .mmx-mini-wave i:nth-child(6) { --mmx-wave-index: 6; } .mmx-mini-wave i:nth-child(7) { --mmx-wave-index: 7; } .mmx-mini-wave i:nth-child(8) { --mmx-wave-index: 8; } .mmx-mini-wave i:nth-child(9) { --mmx-wave-index: 9; } .mmx-mini-wave i:nth-child(10) { --mmx-wave-index: 10; } .mmx-mini-wave i:nth-child(11) { --mmx-wave-index: 11; } .mmx-mini-wave i:nth-child(12) { --mmx-wave-index: 12; } .mmx-mini-wave i:nth-child(13) { --mmx-wave-index: 13; } .mmx-mini-wave i:nth-child(14) { --mmx-wave-index: 14; } .mmx-mini-wave i:nth-child(15) { --mmx-wave-index: 15; } .mmx-mini-wave i:nth-child(16) { --mmx-wave-index: 16; } .mmx-mini-wave i:nth-child(17) { --mmx-wave-index: 17; } .mmx-mini-wave i:nth-child(18) { --mmx-wave-index: 18; } .mmx-mini-wave i:nth-child(19) { --mmx-wave-index: 19; } .mmx-mini-wave i:nth-child(20) { --mmx-wave-index: 20; }
  @keyframes mmx-wave-dance { from { transform: scaleY(.62); } to { transform: scaleY(1); } }
  .mmx-playhead { position: absolute; left: 12%; top: 28px; bottom: 8px; z-index: 4; width: 1px; background: var(--mmx-paper); box-shadow: 0 0 10px rgba(242,196,91,.9); animation: mmx-playhead-sweep var(--mmx-loop-duration) linear infinite; }
  .mmx-playhead::before { content: ''; position: absolute; top: -3px; left: -3px; width: 7px; height: 7px; border-radius: 50%; background: var(--mmx-paper); box-shadow: 0 0 10px var(--mmx-paper); }
  .mmx-playhead-time { position: absolute; top: -19px; left: 5px; display: inline-grid; min-width: 43px; height: 9px; color: var(--mmx-paper); font: 7px var(--font-mono); letter-spacing: .04em; }
  @keyframes mmx-playhead-sweep { 0%,5% { left: 12%; opacity: 0; } 10% { left: 17%; opacity: 1; } 84% { left: 89%; opacity: 1; } 92%,100% { left: 93%; opacity: 0; } }
  .mmx-window-bottom { position: relative; padding: 10px 10px 7px; background: #0b0d12; }
  .mmx-window-summary { display: flex; align-items: center; justify-content: space-between; gap: 12px; }
  .mmx-transport { display: flex; gap: 10px; align-items: center; color: #99a0ae; font: 9px var(--font-mono); }
  .mmx-transport span { color: var(--mmx-cyan); font-size: 13px; } .mmx-transport b { color: #e1e4eb; font-weight: 500; }
  .mmx-edit-status { display: flex; align-items: center; gap: 6px; min-width: 98px; color: var(--mmx-cyan); font: 8px var(--font-mono); letter-spacing: .08em; text-align: right; }
  .mmx-edit-status > i { width: 5px; height: 5px; flex: 0 0 auto; border-radius: 50%; background: var(--mmx-cyan); box-shadow: 0 0 8px var(--mmx-cyan); animation: mmx-signal-pulse 1.8s ease-in-out infinite; }
  .mmx-edit-status--source { animation: mmx-status-source var(--mmx-loop-duration) linear infinite; } .mmx-edit-status--ready { margin-left: -98px; opacity: 0; color: var(--mmx-paper); animation: mmx-status-ready var(--mmx-loop-duration) linear infinite; }
  @keyframes mmx-status-source { 0%,45% { opacity: 1; } 53%,100% { opacity: 0; } } @keyframes mmx-status-ready { 0%,45% { opacity: 0; } 53%,88% { opacity: 1; } 96%,100% { opacity: 0; } }
  .mmx-window-hint { display: block; margin-top: 7px; color: #68717e; font: 8px var(--font-mono); text-align: right; }
  .mmx-float { position: absolute; display: grid; gap: 3px; min-width: 142px; padding: 12px 14px; border: 1px solid rgba(255,255,255,.17); border-radius: 8px; background: rgba(12,14,19,.9); backdrop-filter: blur(8px); box-shadow: 0 15px 30px rgba(0,0,0,.35); font-family: var(--font-mono); }
  .mmx-float span { color: #7c8692; font-size: 8px; letter-spacing: .13em; } .mmx-float b { color: #f3f5f7; font-size: 11px; } .mmx-float i { color: var(--mmx-paper); font-size: 9px; font-style: normal; }
  .mmx-float--style { left: -10px; top: 9%; transform: rotate(-5deg); animation: mmx-float-style var(--mmx-drift-duration) ease-in-out infinite; } .mmx-float--mix { right: -12px; bottom: 18%; transform: rotate(4deg); animation: mmx-float-mix var(--mmx-drift-duration) ease-in-out infinite reverse; } .mmx-float--mix i { color: var(--mmx-cyan); }
  .mmx-float--render { right: 4%; bottom: 3%; min-width: 128px; border-color: rgba(242,196,91,.34); transform: rotate(-3deg); animation: mmx-float-render var(--mmx-loop-duration) ease-in-out infinite; } .mmx-float--render i { color: var(--mmx-cyan); }
  @keyframes mmx-float-style { 0%,100% { transform: translateY(0) rotate(-5deg); } 50% { transform: translateY(-7px) rotate(-3deg); } }
  @keyframes mmx-float-mix { 0%,100% { transform: translateY(0) rotate(4deg); } 50% { transform: translateY(7px) rotate(6deg); } }
  @keyframes mmx-float-render { 0%,42%,100% { transform: translateY(0) rotate(-3deg); opacity: .55; } 52%,86% { transform: translateY(-7px) rotate(-1deg); opacity: 1; } }
  .mmx-stage-a11y { position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0; }

  .mmx-receipts { border-top: 1px solid var(--mmx-line); border-bottom: 1px solid var(--mmx-line); background: #0d1015; }
  .mmx-receipts-grid { display: grid; grid-template-columns: repeat(3, 1fr); }
  .mmx-receipts-grid > div { display: flex; align-items: baseline; gap: 15px; min-height: 98px; padding: 30px 28px; border-right: 1px solid var(--mmx-line); } .mmx-receipts-grid > div:first-child { padding-left: 0; } .mmx-receipts-grid > div:last-child { border-right: 0; }
  .mmx-receipt-value { color: var(--mmx-paper); font: 400 42px var(--font-display); } .mmx-receipt-label { color: #a4a0ac; font: 10px var(--font-mono); letter-spacing: .1em; text-transform: uppercase; }
  .mmx-section { padding: 125px 0 135px; background: #08090e; } .mmx-section--dark { background: #0d1015; }
  .mmx-tag { margin: 0 0 22px; color: var(--mmx-red); }
  .mmx-section-heading { display: grid; grid-template-columns: 1.1fr .8fr; gap: 50px; align-items: end; margin-bottom: 62px; } .mmx-section h2 { font-size: clamp(58px, 7vw, 92px); } .mmx-section-heading > p { max-width: 31rem; margin: 0 0 6px; color: #a7a2af; line-height: 1.65; }
  .mmx-control-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; border: 1px solid var(--mmx-line); background: var(--mmx-line); }
  .mmx-control-card { min-height: 360px; display: flex; flex-direction: column; padding: 28px 28px 25px; background: #101218; transition: background .25s ease, transform .25s ease; } .mmx-control-card:hover { position: relative; z-index: 1; background: #171a21; transform: translateY(-4px); }
  .mmx-control-card--paper { border-top: 2px solid var(--mmx-paper); } .mmx-control-card--song { border-top: 2px solid var(--mmx-cyan); } .mmx-control-card--mix { border-top: 2px solid var(--mmx-red); }
  .mmx-card-number { color: #636b76; font: 10px var(--font-mono); } .mmx-card-icon { display: grid; place-items: center; width: 39px; height: 39px; margin: 34px 0 26px; border: 1px solid var(--mmx-line); border-radius: 50%; color: var(--mmx-paper); font: 22px var(--font-display); } .mmx-control-card--song .mmx-card-icon { color: var(--mmx-cyan); } .mmx-control-card--mix .mmx-card-icon { color: var(--mmx-red); }
  .mmx-control-card h3 { max-width: 13ch; margin: 0; color: #f2eee7; font: 400 28px/.98 var(--font-display); letter-spacing: .02em; text-transform: uppercase; } .mmx-control-card p { margin: 18px 0 30px; color: #a7a2af; font-size: 14px; line-height: 1.6; } .mmx-card-foot { display: flex; justify-content: space-between; gap: 12px; align-items: center; margin-top: auto; padding-top: 18px; border-top: 1px solid var(--mmx-line); font: 9px var(--font-mono); letter-spacing: .08em; } .mmx-card-foot span { color: #69717e; } .mmx-card-foot b { color: #d5d8df; font-weight: 400; text-align: right; }
  .mmx-build-grid { display: grid; grid-template-columns: .85fr 1fr; gap: clamp(60px, 11vw, 150px); align-items: start; } .mmx-build-grid h2 { font-size: clamp(58px, 7vw, 92px); } .mmx-body { max-width: 29rem; margin: 30px 0 0; color: #a7a2af; line-height: 1.7; } .mmx-inline-link { display: inline-flex; gap: 12px; margin-top: 32px; color: var(--mmx-cyan); font: 12px var(--font-mono); text-decoration: none; } .mmx-inline-link span { transition: transform .2s ease; } .mmx-inline-link:hover span { transform: translateX(5px); }
  .mmx-steps { display: grid; gap: 0; margin: 0; padding: 0; list-style: none; border-top: 1px solid var(--mmx-line); } .mmx-steps li { display: grid; grid-template-columns: 64px 1fr; gap: 25px; padding: 24px 0; border-bottom: 1px solid var(--mmx-line); } .mmx-steps li > span { color: var(--mmx-paper); font: 11px var(--font-mono); } .mmx-steps b { display: block; color: #f1edf0; font: 400 28px/.95 var(--font-display); letter-spacing: .03em; text-transform: uppercase; } .mmx-steps p { margin: 8px 0 0; color: #858c98; font-size: 13px; }
  .mmx-final { position: relative; padding: 130px 0 150px; text-align: center; background: radial-gradient(circle at 50% 20%, rgba(242,196,91,.13), transparent 25%), #08090e; } .mmx-final-inner { display: grid; justify-items: center; } .mmx-final .mmx-tag { color: var(--mmx-cyan); } .mmx-final h2 { font-size: clamp(68px, 10vw, 130px); } .mmx-final p:not(.mmx-tag) { max-width: 32rem; margin: 28px auto 0; color: #a7a2af; line-height: 1.65; }
  @media (max-width: 980px) { .mmx-hero-grid { grid-template-columns: 1fr; padding-top: 60px; } .mmx-copy { max-width: 670px; } .mmx-stage { min-height: 620px; } .mmx-section-heading, .mmx-build-grid { grid-template-columns: 1fr; gap: 30px; } .mmx-section-heading { margin-bottom: 45px; } }
  @media (max-width: 700px) { .mmx-wrap { width: min(100% - 30px, 560px); } .mmx-copy h1, .mmx-section h2 { font-size: clamp(58px, 16vw, 86px); } .mmx-hero { min-height: auto; } .mmx-hero-grid { padding: 50px 0 45px; gap: 10px; } .mmx-back { margin-bottom: 24px; } .mmx-stage { min-height: 430px; margin: 10px -8px 0; transform: scale(.98); } .mmx-stage-kicker { left: 3%; top: 1%; gap: 6px; font-size: 7px; } .mmx-stage-kicker > i, .mmx-stage-kicker > span:last-child:not(.mmx-stage-state) { display: none; } .mmx-stage-kicker > .mmx-stage-state { display: inline-grid; min-width: 62px; } .mmx-source-stack { left: -8px; bottom: 1%; transform: scale(.75) rotate(-8deg); transform-origin: bottom left; } .mmx-window { width: calc(100% - 2px); animation-name: mmx-window-drift-mobile; } @keyframes mmx-window-drift-mobile { 0%,100% { transform: translate3d(0,0,0) rotate(1deg); } 50% { transform: translate3d(0,-4px,0) rotate(.5deg); } } .mmx-window-mode { font-size: 7px; } .mmx-timeline { padding: 8px 7px 6px; } .mmx-timeline-head { margin-bottom: 6px; font-size: 7px; } .mmx-phase-rail { gap: 2px; } .mmx-phase { gap: 2px; padding: 2px 3px; } .mmx-phase strong { font-size: 6px; } .mmx-phase b { font-size: 5.5px; letter-spacing: .04em; } .mmx-phase-arrow { font-size: 7px; } .mmx-timeline-ruler { padding-left: 35px; font-size: 6px; } .mmx-track { grid-template-columns: 30px minmax(0, 1fr); gap: 5px; height: 20px; } .mmx-track-label { font-size: 6px; } .mmx-preview-signal, .mmx-preview-cut { font-size: 7px; } .mmx-float { min-width: 120px; padding: 9px 10px; } .mmx-float--style { left: -2px; top: 4%; } .mmx-float--mix { right: -2px; bottom: 17%; } .mmx-float--render { right: 0; bottom: 1%; min-width: 112px; } .mmx-float b { font-size: 9px; } .mmx-preview-title { font-size: clamp(42px, 13vw, 67px); } .mmx-paper-strip { right: 4%; bottom: 13%; } .mmx-paper-strip span { font-size: 8px; } .mmx-preview-hud { gap: 5px; font-size: 7px; } .mmx-timecode, .mmx-playhead-time { min-width: 38px; } .mmx-receipts-grid { grid-template-columns: 1fr; } .mmx-receipts-grid > div, .mmx-receipts-grid > div:first-child { min-height: auto; padding: 20px 0; border-right: 0; border-bottom: 1px solid var(--mmx-line); } .mmx-receipts-grid > div:last-child { border-bottom: 0; } .mmx-receipt-value { font-size: 32px; } .mmx-section { padding: 85px 0 95px; } .mmx-control-grid { grid-template-columns: 1fr; } .mmx-control-card { min-height: 320px; } .mmx-steps li { grid-template-columns: 42px 1fr; gap: 14px; } .mmx-steps b { font-size: 24px; } .mmx-final { padding: 95px 0 115px; } }
  /* The hero proof is a real export, framed like a finished editor deliverable. */
  .mmx-showcase { --mmx-drift-duration: 8s; min-height: 630px; overflow: visible; isolation: isolate; transform: none; }
  .mmx-showcase::before { content: ''; position: absolute; inset: 8% 10%; z-index: -1; border: 1px solid rgba(39,217,195,.09); border-radius: 50%; transform: rotate(-8deg); box-shadow: 0 0 90px rgba(39,217,195,.09) inset; }
  .mmx-showcase .mmx-stage-glow { width: 74%; height: 68%; background: rgba(39,217,195,.13); }
  .mmx-showcase-kicker { position: absolute; left: 1%; top: 1%; z-index: 5; display: flex; align-items: center; gap: 9px; color: rgba(242,238,231,.62); font: 9px var(--font-mono); letter-spacing: .14em; }
  .mmx-showcase-kicker b { color: #d5d9e0; font-weight: 400; }
  .mmx-showcase-kicker > i { width: 24px; height: 1px; background: rgba(255,255,255,.2); }
  .mmx-showcase-kicker > span:last-child { color: var(--mmx-paper); }
  .mmx-showcase-kicker .mmx-live-dot { width: 7px; height: 7px; flex: 0 0 auto; animation: mmx-signal-pulse 1.8s ease-in-out infinite; }
  .mmx-showcase-window { position: relative; z-index: 2; width: min(700px, calc(100% - 26px)); margin: 27px 0 13px; padding: 10px; border: 1px solid rgba(255,255,255,.18); border-radius: 16px; background: linear-gradient(145deg, #1b1e25, #0b0d12 58%); box-shadow: 0 40px 100px rgba(0,0,0,.68), 0 0 0 1px rgba(255,255,255,.04) inset, 0 0 90px rgba(39,217,195,.14); transform-origin: 50% 54%; animation: mmx-showcase-drift var(--mmx-drift-duration) ease-in-out infinite; }
  @keyframes mmx-showcase-drift { 0%,100% { transform: translate3d(0,0,0) rotate(1.2deg); } 50% { transform: translate3d(0,-7px,0) rotate(.45deg); } }
  .mmx-showcase-media { position: relative; aspect-ratio: 16 / 9; overflow: hidden; background: #050609; }
  .mmx-showcase-media::before { content: ''; position: absolute; inset: 0; z-index: 1; pointer-events: none; background: linear-gradient(180deg, rgba(4,7,11,.38), transparent 22%, transparent 70%, rgba(4,7,11,.55)); }
  .mmx-showcase-media::after { content: ''; position: absolute; left: -12%; right: -12%; top: -2px; z-index: 3; height: 2px; pointer-events: none; background: linear-gradient(90deg, transparent, rgba(39,217,195,.78), transparent); box-shadow: 0 0 17px rgba(39,217,195,.8); opacity: 0; animation: mmx-showcase-scan 9s linear infinite; }
  @keyframes mmx-showcase-scan { 0%,7% { top: -2px; opacity: 0; } 15%,82% { opacity: .62; } 93%,100% { top: calc(100% + 2px); opacity: 0; } }
  .mmx-showcase-video { position: relative; z-index: 0; display: block; width: 100%; height: 100%; object-fit: cover; background: #050609; }
  .mmx-showcase-video:focus-visible { outline: 2px solid var(--mmx-paper); outline-offset: -4px; }
  .mmx-media-overlay { position: absolute; z-index: 2; left: 16px; right: 16px; display: flex; align-items: center; justify-content: space-between; gap: 12px; pointer-events: none; color: rgba(248,244,235,.84); font: 8px var(--font-mono); letter-spacing: .14em; text-shadow: 0 2px 10px rgba(0,0,0,.8); }
  .mmx-media-overlay span { padding: 5px 7px 4px; background: rgba(5,7,11,.52); }
  .mmx-media-overlay span:first-child { color: var(--mmx-paper); border-left: 2px solid var(--mmx-paper); }
  .mmx-media-overlay span:last-child { color: #b4f8ef; border-right: 2px solid var(--mmx-cyan); text-align: right; }
  .mmx-media-overlay--top { top: 15px; }
  .mmx-media-overlay--bottom { bottom: 47px; font-size: 7px; letter-spacing: .11em; }
  .mmx-showcase-caption { display: flex; align-items: flex-end; justify-content: space-between; gap: 16px; padding: 16px 17px 15px; background: #0a0d12; border-top: 1px solid rgba(255,255,255,.08); }
  .mmx-showcase-caption > div:first-child { display: grid; gap: 6px; min-width: 0; }
  .mmx-showcase-label { color: var(--mmx-cyan); font: 8px var(--font-mono); letter-spacing: .14em; }
  .mmx-showcase-caption strong { overflow: hidden; color: #f2eee7; font: 400 clamp(23px, 3.1vw, 33px)/.9 var(--font-display); letter-spacing: .03em; text-overflow: ellipsis; white-space: nowrap; }
  .mmx-showcase-spec { display: grid; justify-items: end; gap: 5px; flex: 0 0 auto; font: 8px var(--font-mono); letter-spacing: .11em; text-align: right; }
  .mmx-showcase-spec b { color: var(--mmx-paper); font-weight: 400; }
  .mmx-showcase-spec span { color: #79838e; }
  .mmx-showcase-float { position: absolute; z-index: 4; display: grid; gap: 3px; min-width: 148px; padding: 12px 14px; border: 1px solid rgba(255,255,255,.17); border-radius: 8px; background: rgba(12,14,19,.9); backdrop-filter: blur(8px); box-shadow: 0 15px 30px rgba(0,0,0,.36); font-family: var(--font-mono); }
  .mmx-showcase-float span { color: #7c8692; font-size: 8px; letter-spacing: .13em; }
  .mmx-showcase-float b { color: #f3f5f7; font-size: 11px; font-weight: 500; }
  .mmx-showcase-float i { color: var(--mmx-paper); font-size: 9px; font-style: normal; }
  .mmx-showcase-float--left { left: -10px; top: 21%; transform: rotate(-5deg); animation: mmx-showcase-float-left var(--mmx-drift-duration) ease-in-out infinite; }
  .mmx-showcase-float--right { right: -12px; bottom: 20%; border-color: rgba(242,196,91,.34); transform: rotate(4deg); animation: mmx-showcase-float-right var(--mmx-drift-duration) ease-in-out infinite reverse; }
  .mmx-showcase-float--right i { color: var(--mmx-cyan); }
  @keyframes mmx-showcase-float-left { 0%,100% { transform: translateY(0) rotate(-5deg); } 50% { transform: translateY(-7px) rotate(-3deg); } }
  @keyframes mmx-showcase-float-right { 0%,100% { transform: translateY(0) rotate(4deg); } 50% { transform: translateY(7px) rotate(6deg); } }
  .mmx-showcase-trace { position: absolute; bottom: 1%; left: 50%; z-index: 4; display: flex; align-items: center; justify-content: center; gap: 8px; width: max-content; color: #697480; font: 8px var(--font-mono); letter-spacing: .11em; transform: translateX(-50%); }
  .mmx-showcase-trace i { width: 24px; height: 1px; background: rgba(255,255,255,.17); }
  .mmx-showcase-trace b { color: var(--mmx-paper); font-weight: 400; }

  @media (max-width: 700px) {
    .mmx-showcase { min-height: 412px; margin: 10px -8px 0; }
    .mmx-showcase-window { width: calc(100% - 2px); margin-top: 22px; padding: 7px; border-radius: 12px; animation-name: mmx-showcase-drift-mobile; }
    @keyframes mmx-showcase-drift-mobile { 0%,100% { transform: translate3d(0,0,0) rotate(.8deg); } 50% { transform: translate3d(0,-4px,0) rotate(.25deg); } }
    .mmx-showcase-kicker { left: 3%; top: 0; gap: 6px; font-size: 7px; }
    .mmx-showcase-kicker > i { display: none; }
    .mmx-showcase-kicker > span:last-child { margin-left: auto; }
    .mmx-window-bar { height: 28px; padding: 0 5px 0 3px; font-size: 7px; }
    .mmx-window-brand { gap: 5px; }
    .mmx-window-brand i { width: 6px; height: 6px; }
    .mmx-window-mode { font-size: 6px; }
    .mmx-media-overlay { left: 8px; right: 8px; gap: 5px; font-size: 6px; letter-spacing: .09em; }
    .mmx-media-overlay span { padding: 4px 5px 3px; }
    .mmx-media-overlay--top { top: 8px; }
    .mmx-media-overlay--bottom { bottom: 35px; font-size: 5.5px; }
    .mmx-showcase-caption { gap: 8px; padding: 10px 9px 9px; }
    .mmx-showcase-caption > div:first-child { gap: 5px; }
    .mmx-showcase-label { font-size: 6px; }
    .mmx-showcase-caption strong { font-size: 19px; }
    .mmx-showcase-spec { gap: 3px; font-size: 6px; }
    .mmx-showcase-float { display: none; }
    .mmx-showcase-trace { bottom: 0; gap: 5px; width: 100%; font-size: 6px; }
    .mmx-showcase-trace i { width: 13px; }
  }

  @media (prefers-reduced-motion: reduce) { .mmx-edit-loop { --mmx-loop-duration: 12s; --mmx-drift-duration: 14s; --mmx-wave-duration: 1.8s; } .mmx-showcase-window, .mmx-showcase-float--left, .mmx-showcase-float--right { animation-duration: 14s; } .mmx-button, .mmx-control-card, .mmx-inline-link span { transition: none; } .mmx-button:hover, .mmx-control-card:hover { transform: none; } }
  :global(body:has(.mmx-page) .daily-quote) { display: none; }
</style>
