---
import Layout from '../layouts/Layout.astro';
import Aurora from '../components/Aurora.astro';

const standingsApi = '/api';
---

<style is:global>
.standings-page{--lb-ink: #05050b;--lb-panel: rgba(11, 9, 25, .94);--lb-panel-raised: rgba(17, 14, 36, .97);--lb-line: rgba(218, 220, 255, .13);--lb-text: #f7f4ff;--lb-muted: #aaa6bf;--lb-subtle: #706c84;--lb-cyan: #36e7ff;--lb-magenta: #d96bff;--lb-green: #66f6a3;--lb-red: #ff7b91;--lb-gold: #ffbd4a;--lb-gold-bright: #fff0bd;--lb-silver: #d4e0f0;--lb-bronze: #d99568;background:radial-gradient(circle at 9% 8%,rgba(161,83,255,.14),transparent 28rem),radial-gradient(circle at 88% 36%,rgba(0,212,255,.09),transparent 30rem),linear-gradient(135deg,#05050b,#0c071c 54%,#05060e)}
.standings-page:before{position:absolute;inset:0;background:radial-gradient(circle at 50% 14%,rgba(255,255,255,.035),transparent 32rem);content:"";pointer-events:none}
.sp-grid{opacity:.2;background-size:68px 68px;mask-image:linear-gradient(to bottom,black 0%,rgba(0,0,0,.8) 42%,transparent 76%)}
.sp-shell{width:min(1240px,calc(100% - 48px));padding:30px 0 58px}
.sp-hero{position:relative;max-width:none;padding:86px 0 52px;margin-bottom:30px}
.sp-hero:after{position:absolute;top:4rem;right:8%;width:22rem;height:13rem;border-radius:50%;background:radial-gradient(ellipse,rgba(0,212,255,.1),transparent 68%);content:"";pointer-events:none}
.sp-kicker{width:max-content;padding:7px 10px;border:1px solid rgba(54,231,255,.24);border-radius:999px;background:#36e7ff0b;color:var(--lb-cyan);box-shadow:0 0 26px #36e7ff0f;font-size:10px}
.sp-kicker-dot{width:6px;height:6px;box-shadow:0 0 16px #36e7fff2}
.sp-title-row{position:relative;z-index:1;margin-top:19px}
.sp-title-row h1{font-size:clamp(58px,9.5vw,134px);letter-spacing:-.085em;line-height:.8}
.sp-title-gradient{background:linear-gradient(110deg,#fff1c7,#ffbd4a 27%,#e975ff 60%,#36e7ff);filter:drop-shadow(0 13px 28px rgba(217,107,255,.13))}
.sp-title-gradient{-webkit-background-clip:text;background-clip:text;color:transparent}
.sp-title-mark{color:var(--lb-gold);text-shadow:0 0 24px rgba(255,189,74,.36)}
.sp-title-muted{color:#85819b;opacity:1}
.sp-intro{max-width:700px;margin-top:25px;color:#c4c0d3;font-size:16px;line-height:1.7}
.sp-hero-chip{min-width:194px;padding:17px 18px 15px;border-color:#36e7ff4d;border-radius:12px;background:radial-gradient(circle at 20% 10%,rgba(217,107,255,.18),transparent 58%),linear-gradient(145deg,#36e7ff1c,#ffbd4a12);box-shadow:inset 0 1px #ffffff14,0 18px 48px #0000004d,0 0 34px #36e7ff12;transform:rotate(1deg)}
.sp-chip-top{color:var(--lb-cyan)}
.sp-chip-main{font-size:27px;text-shadow:0 0 18px rgba(255,255,255,.12)}
.sp-chip-bottom{color:#aaa6bf}
.sp-stage{position:relative;border-color:#36e7ff47;border-radius:18px;background:linear-gradient(138deg,#180e32fa,#0a0818fa 54%,#091123fa),var(--lb-panel);box-shadow:0 34px 100px #00000073,0 0 46px #36e7ff0d,inset 0 1px #ffffff12}
.sp-stage:before{position:absolute;inset:0 12% auto 0;height:1px;background:linear-gradient(90deg,rgba(54,231,255,.85),rgba(217,107,255,.4),transparent);content:"";pointer-events:none}
.sp-stage:after{position:absolute;top:-11rem;right:-6rem;width:30rem;height:26rem;border-radius:50%;background:radial-gradient(ellipse,rgba(217,107,255,.1),transparent 68%);content:"";pointer-events:none}
.sp-stage>*{position:relative;z-index:1}
.sp-stage-topline{padding:31px 34px 24px;background:linear-gradient(105deg,rgba(54,231,255,.06),transparent 42%,rgba(217,107,255,.065))}
.sp-stage .sp-section-label{color:var(--lb-cyan)}
.sp-stage h2{font-size:clamp(25px,3vw,36px);letter-spacing:-.05em}
.sp-board-context{max-width:680px;margin-top:10px;color:#aaa6bf}
.sp-board-context i{color:var(--lb-magenta);text-shadow:0 0 12px rgba(217,107,255,.6)}
.sp-stage-actions{justify-content:flex-end}
.sp-processed-chip{padding:8px 11px;border-color:#66f6a352;border-radius:999px;background:#66f6a311;color:var(--lb-green);box-shadow:0 0 20px #66f6a30d}
.sp-quiet-button,.sp-refresh-button,.sp-segmented button{border-color:#ffffff29;border-radius:8px;background:#ffffff0b;color:#c7c3d5;box-shadow:inset 0 1px #ffffff0a}
.sp-refresh-button{border-color:#36e7ff6b;background:#36e7ff1b;color:#d1fbff;box-shadow:0 0 24px #36e7ff12,inset 0 1px #ffffff14}
.sp-quiet-button:hover,.sp-refresh-button:hover,.sp-segmented button:hover{border-color:#36e7ffb8;background:#36e7ff29;box-shadow:0 0 24px #36e7ff1a}
.sp-controls{padding:23px 34px 15px;background:#03030c29}
.sp-control{color:#8d89a1}
.sp-control select{border-color:#ffffff26;border-radius:8px;background:#050511e6;color:#f1eefb;box-shadow:inset 0 1px #ffffff0a,0 9px 22px #0000001f}
.sp-control select:focus{border-color:#36e7ff94;box-shadow:0 0 0 3px #36e7ff14}
.sp-segmented{overflow:hidden;border:1px solid rgba(255,255,255,.15);border-radius:8px}
.sp-segmented button{border:0;border-radius:0}
.sp-segmented button+button{border-left:1px solid rgba(255,255,255,.1)}
.sp-segmented button.is-active{border-color:transparent;background:linear-gradient(135deg,#ffbd4a45,#d96bff24);color:#fff0c1;box-shadow:0 0 24px #ffbd4a1f inset,0 0 18px #ffbd4a0f}
.sp-status-line{min-height:42px;padding:0 34px 17px;border-bottom-color:#ffffff14;color:#aaa6bf}
.sp-status-dot--good{background:var(--lb-green);box-shadow:0 0 15px #66f6a3d9}
.sp-status-dot--warn{background:var(--lb-gold);box-shadow:0 0 15px #ffbd4ab3}
.sp-status-delay{color:#77738b}
.sp-summary{grid-template-columns:1.2fr .8fr 1.1fr .8fr 1.2fr;border-top-color:#ffffff14;border-bottom-color:#ffffff14;background:linear-gradient(90deg,#36e7ff06,#0003 42%,#d96bff09)}
.sp-summary-item{min-height:76px;padding:13px 18px;border-right-color:#ffffff14}
.sp-summary-label{color:#77738b}
.sp-summary-item strong{color:#eeeaf8;font-size:13px}
.sp-summary-item--live{background:linear-gradient(90deg,rgba(102,246,163,.075),transparent)}
.sp-summary-item--live strong{color:var(--lb-green);text-shadow:0 0 17px rgba(102,246,163,.23)}
.sp-board{padding:0 34px 32px}
.sp-board-head,.sp-row{grid-template-columns:72px minmax(190px,1.35fr) 104px 72px 130px minmax(150px,.9fr);gap:13px}
.sp-board-head{padding:20px 17px 11px;color:#77738b;font-size:8px}
.sp-rows{gap:10px}
.sp-row{min-height:84px;padding:13px 17px 13px 16px;border-color:#dadcff1f;border-radius:10px;background:linear-gradient(105deg,#ffffff0b,#080714d6 54%,#0b0a19eb);box-shadow:0 12px 28px #0000002b,inset 0 1px #ffffff09}
.sp-row>*{position:relative;z-index:1}
.sp-row:before{inset:5px auto 5px -1px;width:3px;border-radius:10px;background:#ffffff40}
.sp-row:after{position:absolute;inset:0;background:radial-gradient(ellipse at 0% 50%,rgba(54,231,255,.08),transparent 43%);content:"";pointer-events:none}
.sp-row:hover{transform:translateY(-2px) translate(2px);border-color:#dadcff47;background:linear-gradient(105deg,#ffffff13,#0b0a1beb 54%,#0d0d1ff2);box-shadow:0 17px 34px #00000038,0 0 24px #36e7ff0b}
.sp-row--rank-1{min-height:96px;border-color:#ffbd4ab8;background:linear-gradient(105deg,#ffbd4a47,#ffbd4a18 30%,#d96bff11 64%,#0b0a19e6);box-shadow:0 0 0 1px #ffbd4a14,0 18px 45px #ffbd4a1c,0 0 38px #d96bff0d,inset 0 1px #fff2c233}
.sp-row--rank-1:before{width:4px;background:linear-gradient(#fff5c8,var(--lb-gold),var(--lb-magenta));box-shadow:0 0 18px #ffbd4ae6}
.sp-row--rank-1:after{background:radial-gradient(ellipse at 0% 50%,rgba(255,239,174,.22),transparent 39%),radial-gradient(ellipse at 78% 0%,rgba(217,107,255,.1),transparent 38%)}
.sp-row--rank-2{border-color:#d4e0f06b;background:linear-gradient(105deg,#d4e0f024,#d4e0f009 39%,#0b0a19e6);box-shadow:0 0 26px #d4e0f00e,inset 0 1px #f0f8ff1c}
.sp-row--rank-2:before{background:linear-gradient(#f3f8ff,var(--lb-silver));box-shadow:0 0 12px #d4e0f08c}
.sp-row--rank-3{border-color:#d9956875;background:linear-gradient(105deg,#d9956826,#d9956809 39%,#0b0a19e6);box-shadow:0 0 26px #d995680e,inset 0 1px #ffdcc51a}
.sp-row--rank-3:before{background:linear-gradient(#ffd0ad,var(--lb-bronze));box-shadow:0 0 12px #d995688c}
.sp-movement{color:#8c889f;font-size:12px}
.sp-movement-arrow{color:currentColor;text-shadow:0 0 14px currentColor}
.sp-player{gap:13px}
.sp-player strong{color:#f3effd;font-size:16px;text-shadow:0 0 16px rgba(255,255,255,.06)}
.sp-player-copy{gap:5px}
.sp-player-copy small{color:#9691aa;font-size:9px}
.sp-row--rank-1 .sp-player strong{color:var(--lb-gold-bright);text-shadow:0 0 20px rgba(255,189,74,.34)}
.sp-row--rank-1 .sp-player-copy small{color:#d7c5a0}
.sp-rank{min-width:45px;height:30px;border:1px solid rgba(255,255,255,.14);border-radius:6px;background:#ffffff12}
.sp-row--rank-1 .sp-rank{border-color:#ffbd4a7a;background:#ffbd4a3b;color:var(--lb-gold-bright);box-shadow:0 0 18px #ffbd4a1f}
.sp-row--rank-2 .sp-rank{border-color:#d4e0f04d;background:#d4e0f026;color:#eff5fd}
.sp-row--rank-3 .sp-rank{border-color:#d9956857;background:#d995682e;color:#ffd0ad}
.sp-points strong{color:#f5f2fc;font-size:23px;text-shadow:0 0 14px rgba(255,255,255,.07)}
.sp-row--rank-1 .sp-points strong{color:var(--lb-gold-bright);text-shadow:0 0 20px rgba(255,189,74,.46)}
.sp-points span,.sp-wins span,.sp-prize span{color:#77738b}
.sp-wins strong{color:#ece8f7;font-size:16px}
.sp-row--rank-1 .sp-wins strong{color:#fff2c8}
.sp-prize strong{color:var(--lb-green);text-shadow:0 0 16px rgba(102,246,163,.18)}
.sp-prize--tier-1 strong{color:var(--lb-gold-bright);text-shadow:0 0 18px rgba(255,189,74,.52)}
.sp-prize--tier-2 strong{color:var(--lb-silver);text-shadow:0 0 16px rgba(212,224,240,.35)}
.sp-prize--tier-3 strong{color:#ffc09a;text-shadow:0 0 16px rgba(217,149,104,.38)}
.sp-gap{color:#ffd47f}
.sp-last{color:var(--lb-green)}
.sp-footnote{position:relative;margin:0 34px 32px;padding:16px 18px;border-color:#36e7ff33;border-radius:10px;background:linear-gradient(105deg,#36e7ff14,#36e7ff06 48%,#d96bff09);box-shadow:0 0 28px #36e7ff09,inset 0 1px #ffffff0b}
.sp-footnote strong{color:#d7fbff}
.sp-footnote-mark{border-color:#36e7ffa3;color:var(--lb-cyan);box-shadow:0 0 14px #36e7ff1f}
.sp-lower-grid{gap:16px;margin-top:18px}
.sp-info-card{position:relative;overflow:hidden;min-height:204px;padding:27px 29px;border-color:#ffffff1f;border-radius:14px;background:linear-gradient(135deg,#140d2af2,#080813f5 70%);box-shadow:0 22px 52px #00000040,inset 0 1px #ffffff0e}
.sp-info-card>*{position:relative;z-index:1}
.sp-info-card:before{position:absolute;inset:0 28% auto 0;height:2px;background:linear-gradient(90deg,var(--lb-gold),transparent);content:""}
.sp-info-card:after{position:absolute;right:-4rem;bottom:-7rem;width:19rem;height:16rem;border-radius:50%;background:radial-gradient(ellipse,rgba(255,189,74,.13),transparent 68%);content:"";pointer-events:none}
.sp-info-card--cyan{border-color:#36e7ff33;background:linear-gradient(135deg,#0b1b2eeb,#080813f5 70%)}
.sp-info-card--cyan:before{background:linear-gradient(90deg,var(--lb-cyan),transparent)}
.sp-info-card--cyan:after{background:radial-gradient(ellipse,rgba(54,231,255,.13),transparent 68%)}
.sp-info-number{align-self:start;color:var(--lb-gold);font-family:var(--font-display);font-size:48px;letter-spacing:-.04em;line-height:.82;text-shadow:0 0 20px rgba(255,189,74,.18)}
.sp-info-card--cyan .sp-info-number{color:var(--lb-cyan);text-shadow:0 0 20px rgba(54,231,255,.18)}
.sp-info-card .sp-section-label{color:var(--lb-gold)}
.sp-info-card--cyan .sp-section-label{color:var(--lb-cyan)}
.sp-info-card h2{font-size:29px;letter-spacing:-.05em}
.sp-info-card p:last-child{margin-top:15px;color:#b9b4c9;font-size:13px;line-height:1.68}
.sp-footer-line{margin-top:34px;padding-top:20px;border-top-color:#ffffff1a;color:#8d89a1}
.sp-footer-line a{color:var(--lb-cyan);text-shadow:0 0 14px rgba(54,231,255,.16)}
.sp-footer-line a:hover{color:#edfdff}
@media(max-width:980px){.sp-summary{grid-template-columns:repeat(3,1fr)}
.sp-summary-item:nth-child(3){border-right:0}
.sp-summary-item:nth-child(-n+3){border-bottom:1px solid rgba(255,255,255,.08)}
.sp-summary-item--source{grid-column:1 / -1}
}
@media(max-width:820px){.sp-shell{width:min(100% - 28px,700px);padding-top:20px}
.sp-hero{padding-top:66px}
.sp-title-row{align-items:start;flex-direction:column}
.sp-hero-chip{align-self:start}
.sp-stage-topline{align-items:start;flex-direction:column;padding:25px 21px 20px}
.sp-stage-actions{width:100%;justify-content:start}
.sp-controls{grid-template-columns:1fr 1fr;padding:18px 21px 14px}
.sp-control--wide{grid-column:1 / -1;grid-row:1}
.sp-control--view{grid-column:2}
.sp-status-line{padding-left:21px;padding-right:21px}
.sp-summary{grid-template-columns:1fr 1fr}
.sp-summary-item:nth-child(2){border-right:0}
.sp-summary-item:nth-child(-n+2){border-bottom:1px solid rgba(255,255,255,.08)}
.sp-summary-item--source{grid-column:1 / -1}
.sp-board{padding-left:21px;padding-right:21px}
.sp-board-head{display:none}
.sp-row{grid-template-columns:48px minmax(110px,1fr) 82px;grid-template-areas:"move player points" "move math math";gap:0 9px;min-height:86px;padding:12px}
.sp-row--rank-1{min-height:94px}
.sp-movement{grid-area:move}
.sp-player{grid-area:player}
.sp-points{grid-area:points}
.sp-wins,.sp-prize{display:none}
.sp-math{grid-area:math;flex-direction:row;justify-content:space-between;align-items:center;padding-top:7px}
.sp-points{flex-direction:column;align-items:end;gap:1px}
.sp-footnote{margin-left:21px;margin-right:21px}
.sp-lower-grid{grid-template-columns:1fr}
}
@media(max-width:520px){.sp-shell{width:min(100% - 20px,500px);padding-top:14px}
.sp-hero{padding-top:56px;padding-bottom:42px}
.sp-title-row h1{font-size:62px}
.sp-intro{font-size:14px}
.sp-stage-actions{display:flex;flex-wrap:wrap;gap:7px}
.sp-processed-chip{flex:0 0 auto}
.sp-quiet-button,.sp-refresh-button{flex:1 1 calc(50% - 7px)}
.sp-controls{grid-template-columns:1fr}
.sp-control--wide,.sp-control--view{grid-column:auto;grid-row:auto}
.sp-summary-item{min-height:64px;padding:11px 13px}
.sp-summary-item strong{font-size:10px}
.sp-row{grid-template-columns:42px minmax(84px,1fr) 74px;padding-left:9px;padding-right:9px}
.sp-row--rank-1{min-height:91px}
.sp-movement{gap:2px;font-size:10px}
.sp-movement-arrow{font-size:20px}
.sp-rank{min-width:35px}
.sp-player{gap:6px}
.sp-player strong{font-size:12px}
.sp-player-copy small{font-size:8px}
.sp-gap,.sp-last{font-size:9px}
.sp-info-card{min-height:0;padding:21px}
.sp-info-number{font-size:42px}
.sp-footer-line{align-items:start;flex-direction:column}
}
.sp-overlay-mode{background:#05050b}
.sp-overlay-mode:before,.sp-overlay-mode .sp-stage:after{display:none}
.sp-overlay-mode .sp-stage{border-color:#36e7ff61;border-radius:10px;background:#070811f5;box-shadow:0 16px 40px #00000080,0 0 22px #36e7ff0f}
.sp-overlay-mode .sp-stage:before{display:none}
.sp-overlay-mode .sp-row{min-height:64px;padding:8px 9px;border-radius:7px;background:#ffffff09;box-shadow:none}
.sp-overlay-mode .sp-row--rank-1{min-height:64px;background:linear-gradient(100deg,#ffbd4a2e,#ffbd4a0a 55%,#ffffff06);box-shadow:0 0 22px #ffbd4a14}
.sp-overlay-mode .sp-row:hover{transform:none}
.sp-overlay-mode .sp-row:after{opacity:.3}
@media(prefers-reduced-motion:reduce){.sp-title-gradient{filter:none}
.sp-row{animation:none;transition:none}
.sp-quiet-button,.sp-refresh-button,.sp-segmented button{transition:none}
}
</style>

<style is="global">
  .standings-page .sp-rules {
    margin: 0 29px 24px;
    overflow: hidden;
    border: 1px solid rgba(107, 220, 255, .28);
    border-radius: 16px;
    background: radial-gradient(circle at 8% 0, rgba(107, 220, 255, .13), transparent 33%), linear-gradient(135deg, rgba(19, 28, 48, .94), rgba(10, 14, 28, .97) 52%, rgba(27, 17, 47, .94));
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, .1), 0 16px 36px rgba(0, 0, 0, .24), 0 0 30px rgba(107, 220, 255, .07);
  }

  .standings-page .sp-rules summary {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    gap: 13px;
    min-height: 64px;
    padding: 0 18px;
    cursor: pointer;
    list-style: none;
  }

  .standings-page .sp-rules summary::-webkit-details-marker { display: none; }
  .standings-page .sp-rules summary::before { width: 7px; height: 7px; border-radius: 50%; background: var(--lb-cyan); box-shadow: 0 0 14px rgba(107, 220, 255, .82); content: ''; }
  .standings-page .sp-rules-title { color: #f2fbff; font: 700 10px var(--font-mono); letter-spacing: .12em; text-transform: uppercase; text-shadow: 0 0 16px rgba(107, 220, 255, .24); }
  .standings-page .sp-rules-summary { overflow: hidden; color: var(--lb-muted); font: 500 9px var(--font-mono); text-align: right; text-overflow: ellipsis; white-space: nowrap; }
  .standings-page .sp-rules-chevron { color: var(--lb-cyan); font: 700 17px var(--font-mono); line-height: 1; transform: translateY(-2px); transition: transform 180ms ease; }
  .standings-page .sp-rules[open] .sp-rules-chevron { transform: rotate(180deg) translateY(2px); }
  .standings-page .sp-rules[open] summary { border-bottom: 1px solid rgba(255, 255, 255, .08); }

  .standings-page .sp-rules-grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 9px; padding: 14px 16px 0; }
  .standings-page .sp-rule { min-width: 0; padding: 13px 14px; border: 1px solid rgba(107, 220, 255, .14); border-radius: 11px; background: rgba(4, 7, 16, .46); box-shadow: inset 0 1px 0 rgba(255, 255, 255, .045); }
  .standings-page .sp-rule span { display: block; margin-bottom: 7px; color: var(--lb-subtle); font: 600 8px var(--font-mono); letter-spacing: .1em; text-transform: uppercase; }
  .standings-page .sp-rule strong { display: block; overflow-wrap: anywhere; color: #a9f2ff; font: 700 11px/1.35 var(--font-mono); text-shadow: 0 0 16px rgba(107, 220, 255, .3); }
  .standings-page .sp-rules-note { margin: 14px 16px 16px; padding-top: 12px; border-top: 1px solid rgba(255, 255, 255, .08); color: var(--lb-muted); font: 500 10px/1.55 var(--font-mono); }
  .standings-page .sp-prize-source { display: inline-block; width: fit-content; color: var(--lb-gold-bright); font: 600 8px var(--font-mono); text-decoration: none; text-shadow: 0 0 12px rgba(242, 189, 87, .24); }
  .standings-page .sp-prize-source:hover { color: #fff0bf; text-decoration: underline; }

  .standings-page .sp-empty {
    display: grid;
    place-items: center;
    gap: 9px;
    min-height: 162px;
    padding: 28px 20px;
    border: 1px dashed rgba(107, 220, 255, .24);
    border-radius: 12px;
    background: radial-gradient(circle at 50% 0, rgba(107, 220, 255, .08), transparent 54%), rgba(4, 7, 16, .32);
    color: var(--lb-muted);
    text-align: center;
  }

  .standings-page .sp-empty[hidden] { display: none; }
  .standings-page .sp-empty-icon { display: grid; place-items: center; width: 38px; height: 38px; border: 1px solid rgba(107, 220, 255, .35); border-radius: 50%; color: var(--lb-cyan); box-shadow: 0 0 25px rgba(107, 220, 255, .14); font: 700 20px var(--font-mono); }
  .standings-page .sp-empty strong { color: var(--lb-text); font-size: 15px; }
  .standings-page .sp-empty > span:not(.sp-empty-icon) { max-width: 620px; color: var(--lb-muted); font: 500 10px/1.6 var(--font-mono); }

  .standings-page .sp-future { width: min(100%, 790px); margin-top: 8px; text-align: left; }
  .standings-page .sp-future-heading { display: flex; align-items: center; justify-content: space-between; gap: 10px; margin-bottom: 8px; color: var(--lb-cyan); font: 700 8px var(--font-mono); letter-spacing: .13em; }
  .standings-page .sp-future-heading span:last-child { color: var(--lb-subtle); font-weight: 500; letter-spacing: .06em; }
  .standings-page .sp-future-list { display: grid; gap: 7px; }
  .standings-page .sp-future-item { display: grid; grid-template-columns: minmax(0, 1fr) auto auto; align-items: center; gap: 15px; width: 100%; padding: 10px 12px; border: 1px solid rgba(107, 220, 255, .14); border-radius: 8px; cursor: pointer; color: var(--lb-text); background: rgba(107, 220, 255, .045); text-align: left; transition: border-color 180ms ease, background 180ms ease, transform 180ms ease, box-shadow 180ms ease; }
  .standings-page .sp-future-item:hover { border-color: rgba(107, 220, 255, .48); background: rgba(107, 220, 255, .1); box-shadow: 0 0 22px rgba(107, 220, 255, .1); transform: translateY(-1px); }
  .standings-page .sp-future-copy { display: grid; min-width: 0; gap: 4px; }
  .standings-page .sp-future-copy strong { overflow: hidden; color: #dffaff; font-size: 11px; text-overflow: ellipsis; white-space: nowrap; }
  .standings-page .sp-future-copy small { overflow: hidden; color: #83b3ca; font: 500 9px var(--font-mono); text-overflow: ellipsis; white-space: nowrap; }
  .standings-page .sp-future-schedule { color: var(--lb-gold-bright); font: 600 9px var(--font-mono); text-align: right; white-space: nowrap; }
  .standings-page .sp-future-arrow { color: var(--lb-cyan); font: 700 16px var(--font-mono); text-shadow: 0 0 12px rgba(107, 220, 255, .55); }
  .standings-page .sp-overlay-mode .sp-rules { display: none; }

  @media (max-width: 820px) {
    .standings-page .sp-rules { margin-left: 20px; margin-right: 20px; }
    .standings-page .sp-rules-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  }

  @media (max-width: 520px) {
    .standings-page .sp-rules { margin-left: 15px; margin-right: 15px; }
    .standings-page .sp-rules summary { gap: 8px; padding: 0 11px; }
    .standings-page .sp-rules-summary { font-size: 8px; }
    .standings-page .sp-rules-grid { grid-template-columns: 1fr; padding-left: 11px; padding-right: 11px; }
    .standings-page .sp-rules-note { margin-left: 11px; margin-right: 11px; }
    .standings-page .sp-future-item { grid-template-columns: minmax(0, 1fr) auto; gap: 7px 10px; }
    .standings-page .sp-future-schedule { grid-column: 1; grid-row: 2; text-align: left; }
    .standings-page .sp-future-arrow { grid-column: 2; grid-row: 1 / span 2; }
  }
</style>

<style is:global>
  /* Final hierarchy pass: keep the board in the first viewport and let rank
     color carry the visual story without turning every row into a medal. */

  /* This page hides the site chrome, including the custom cursor. Restore the
     native affordance here instead of inheriting the global cursor suppression. */
  .standings-page :where(button, select, a) {
    cursor: pointer;
  }

  .standings-page .sp-shell {
    padding: 18px 0 58px;
  }

  .standings-page .sp-hero {
    max-width: none;
    padding: 12px 0 18px;
    margin-bottom: 0;
  }

  .standings-page .sp-hero::after {
    display: none;
  }

  .standings-page .sp-kicker {
    padding: 6px 10px;
    border-color: rgba(54, 231, 255, .22);
    background: rgba(54, 231, 255, .04);
    box-shadow: 0 0 22px rgba(54, 231, 255, .08);
  }

  .standings-page .sp-stage-topline {
    padding: 28px 34px 23px;
  }

  .standings-page .sp-summary {
    grid-template-columns: repeat(6, minmax(0, 1fr));
  }

  .standings-page .sp-summary-item--prize {
    background: linear-gradient(135deg, rgba(255, 189, 74, .14), rgba(255, 189, 74, .025));
  }

  .standings-page .sp-summary-item--prize strong {
    color: #ffd36f;
    text-shadow: 0 0 16px rgba(255, 189, 74, .3);
  }

  .standings-page .sp-summary-item--prize strong.is-published {
    color: #fff0bd;
    text-shadow: 0 0 20px rgba(255, 189, 74, .55);
  }

  .standings-page .sp-summary-item--prize strong.is-unpublished {
    color: #b4a77e;
    text-shadow: none;
  }

  .standings-page .sp-row--rank-1 {
    border-color: rgba(255, 203, 79, .88);
    background:
      radial-gradient(ellipse at 9% 50%, rgba(255, 234, 146, .3), transparent 34%),
      radial-gradient(ellipse at 83% 0%, rgba(225, 91, 255, .17), transparent 42%),
      linear-gradient(105deg, rgba(255, 189, 74, .29), rgba(104, 53, 21, .25) 42%, rgba(12, 10, 28, .98));
    box-shadow:
      0 0 0 1px rgba(255, 233, 151, .2),
      0 18px 44px rgba(255, 171, 34, .3),
      0 0 52px rgba(255, 189, 74, .2),
      inset 0 1px rgba(255, 249, 205, .42);
  }

  .standings-page .sp-row--rank-1::before {
    width: 4px;
    background: linear-gradient(#fff9d3, #ffbd4a 54%, #e25bff);
    box-shadow: 0 0 22px rgba(255, 189, 74, .95), 0 0 38px rgba(226, 91, 255, .4);
  }

  .standings-page .sp-row--rank-1::after {
    background:
      radial-gradient(ellipse at 0 50%, rgba(255, 244, 188, .32), transparent 38%),
      radial-gradient(ellipse at 78% 0%, rgba(226, 91, 255, .15), transparent 40%);
  }

  .standings-page .sp-row--rank-2 {
    border-color: rgba(224, 235, 249, .8);
    background:
      radial-gradient(ellipse at 8% 50%, rgba(225, 239, 255, .2), transparent 34%),
      linear-gradient(105deg, rgba(212, 224, 240, .2), rgba(91, 110, 151, .13) 42%, rgba(11, 10, 26, .98));
    box-shadow:
      0 0 0 1px rgba(238, 247, 255, .12),
      0 15px 38px rgba(159, 188, 224, .18),
      0 0 34px rgba(212, 224, 240, .14),
      inset 0 1px rgba(248, 252, 255, .34);
  }

  .standings-page .sp-row--rank-2::before {
    background: linear-gradient(#ffffff, var(--lb-silver));
    box-shadow: 0 0 18px rgba(212, 224, 240, .85), 0 0 30px rgba(182, 208, 239, .3);
  }

  .standings-page .sp-row--rank-3 {
    border-color: rgba(236, 163, 115, .82);
    background:
      radial-gradient(ellipse at 8% 50%, rgba(255, 198, 150, .2), transparent 34%),
      linear-gradient(105deg, rgba(217, 149, 104, .22), rgba(105, 59, 55, .14) 42%, rgba(12, 10, 26, .98));
    box-shadow:
      0 0 0 1px rgba(255, 214, 178, .12),
      0 15px 38px rgba(217, 120, 69, .2),
      0 0 34px rgba(217, 149, 104, .15),
      inset 0 1px rgba(255, 225, 201, .28);
  }

  .standings-page .sp-row--rank-3::before {
    background: linear-gradient(#ffd8ba, var(--lb-bronze));
    box-shadow: 0 0 18px rgba(217, 149, 104, .82), 0 0 30px rgba(217, 113, 74, .28);
  }

  .standings-page .sp-row--standard {
    border-color: rgba(113, 182, 246, .48);
    background:
      radial-gradient(ellipse at 8% 50%, rgba(85, 197, 255, .16), transparent 34%),
      linear-gradient(105deg, rgba(50, 111, 184, .23), rgba(43, 54, 117, .18) 46%, rgba(10, 10, 28, .97));
    box-shadow:
      0 12px 32px rgba(24, 67, 139, .28),
      0 0 26px rgba(47, 142, 232, .1),
      inset 0 1px rgba(174, 222, 255, .15);
  }

  .standings-page .sp-row--standard::before {
    background: linear-gradient(#a6ecff, #5276ff);
    box-shadow: 0 0 16px rgba(82, 155, 255, .76), 0 0 28px rgba(54, 231, 255, .24);
  }

  .standings-page .sp-row--standard::after {
    background: radial-gradient(ellipse at 0 50%, rgba(94, 188, 255, .2), transparent 43%);
  }

  .standings-page .sp-row--standard .sp-player strong {
    color: #d9ecff;
    text-shadow: 0 0 16px rgba(106, 204, 255, .22);
  }

  .standings-page .sp-row--standard .sp-player-copy small {
    color: #8cb1d7;
  }

  .standings-page .sp-row--standard .sp-rank {
    border-color: rgba(112, 194, 255, .38);
    background: rgba(74, 145, 224, .18);
    color: #b1e5ff;
    box-shadow: 0 0 16px rgba(67, 157, 247, .14);
  }

  .standings-page .sp-row--rank-1 .sp-points strong {
    color: #fff0bd;
    text-shadow: 0 0 22px rgba(255, 189, 74, .7), 0 0 38px rgba(255, 215, 109, .25);
  }

  .standings-page .sp-row--rank-2 .sp-points strong {
    color: #eff6ff;
    text-shadow: 0 0 20px rgba(212, 224, 240, .55);
  }

  .standings-page .sp-row--rank-3 .sp-points strong {
    color: #ffd0ad;
    text-shadow: 0 0 20px rgba(217, 149, 104, .56);
  }

  .standings-page .sp-row--standard .sp-points strong {
    color: #aee0ff;
    text-shadow: 0 0 18px rgba(83, 183, 255, .46);
  }

  .standings-page .sp-row--standard .sp-prize strong {
    color: #9ed2ff;
    text-shadow: 0 0 16px rgba(83, 183, 255, .36);
  }

  .standings-page .sp-segmented button[data-limit="10"] {
    color: #d5b566;
  }

  .standings-page .sp-segmented button[data-limit="10"].is-active {
    border-color: #ffdc72;
    background: linear-gradient(135deg, #fff2ad 0%, #ffc94f 38%, #e68a1e 100%);
    color: #241306;
    box-shadow:
      0 0 0 1px rgba(255, 238, 160, .58),
      0 0 28px rgba(255, 190, 50, .7),
      0 0 48px rgba(255, 164, 35, .23),
      inset 0 1px rgba(255, 255, 255, .86);
    text-shadow: none;
  }

  @media (max-width: 980px) {
    .standings-page .sp-summary {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .standings-page .sp-summary-item:nth-child(3) {
      border-right: 0;
    }

    .standings-page .sp-summary-item:nth-child(-n+3) {
      border-bottom: 1px solid rgba(255, 255, 255, .08);
    }
  }

  @media (max-width: 820px) {
    .standings-page .sp-shell {
      padding-top: 18px;
    }

    .standings-page .sp-hero {
      padding-top: 10px;
    }

    .standings-page .sp-stage-topline {
      padding: 23px 20px 18px;
    }

    .standings-page .sp-summary {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .standings-page .sp-summary-item:nth-child(2) {
      border-right: 0;
    }

    .standings-page .sp-summary-item:nth-child(-n+2) {
      border-bottom: 1px solid rgba(255, 255, 255, .08);
    }
  }

  @media (max-width: 520px) {
    .standings-page .sp-shell {
      width: min(100% - 20px, 500px);
      padding-top: 14px;
    }

    .standings-page .sp-hero {
      padding-top: 8px;
      padding-bottom: 12px;
    }

    .standings-page .sp-kicker {
      font-size: 9px;
    }

    .standings-page .sp-stage-topline {
      padding: 20px 15px 16px;
    }

    .standings-page .sp-summary-item {
      min-height: 63px;
      padding: 11px 12px;
    }

    .standings-page .sp-summary-item strong {
      font-size: 10px;
    }
  }
</style>

<Layout
  title="Tournament Standings — Maxxtopia"
  description="A live, replay-aware tournament leaderboard for Fortnite watch parties and stream overlays."
  hideChrome
>
  <div class="standings-page maxxtopia-motion-required" data-motion-required="true">
    <Aurora intensity="low" motionRequired />
    <div class="sp-grid" aria-hidden="true"></div>

    <div class="sp-shell">
      <header class="sp-hero">
        <div class="sp-kicker"><span class="sp-kicker-dot"></span> Watch party control room</div>
      </header>

      <section class="sp-stage" aria-labelledby="sp-stage-title">
        <div class="sp-stage-topline">
          <div>
            <p class="sp-section-label">Tournament board</p>
            <h2 id="sp-stage-title">The board between games.</h2>
            <div class="sp-board-context"><span id="sp-board-event">Loading event</span><i aria-hidden="true">/</i><span id="sp-board-round">Loading round</span></div>
          </div>
          <div class="sp-stage-actions">
            <span class="sp-processed-chip" id="sp-board-status">WAITING FOR DATA</span>
            <button class="sp-quiet-button" id="sp-share" type="button">Copy board link</button>
            <button class="sp-quiet-button" id="sp-copy-overlay" type="button">Copy OBS URL</button>
            <button class="sp-refresh-button" id="sp-open-overlay" type="button">Open overlay <span aria-hidden="true">↗</span></button>
            <button class="sp-refresh-button" id="sp-refresh" type="button"><span aria-hidden="true">↻</span> Refresh</button>
          </div>
        </div>

        <div class="sp-overlay-tools" aria-label="Overlay controls">
          <span class="sp-overlay-brand"><span class="sp-overlay-mark">M</span> STANDINGS <i>•</i> AFTER GAME</span>
          <div class="sp-overlay-tool-actions">
            <div class="sp-segmented sp-segmented--overlay" role="group" aria-label="Overlay board size">
              <button type="button" data-limit="5" aria-pressed="true">5</button>
              <button type="button" data-limit="10" aria-pressed="false">10</button>
            </div>
            <button class="sp-overlay-collapse" id="sp-overlay-collapse" type="button" aria-expanded="true"><span id="sp-overlay-collapse-label">Minimize</span><span aria-hidden="true">⌃</span></button>
            <a class="sp-overlay-full" id="sp-full-board" href="/standings/">Full board <span aria-hidden="true">↗</span></a>
          </div>
        </div>

        <div class="sp-controls">
          <label class="sp-control">
            <span>Region</span>
            <select id="sp-region">
              <option value="ALL">All regions</option>
              <option value="NAC">NAC</option>
              <option value="NAW">NAW</option>
              <option value="EU">EU</option>
              <option value="BR">BR</option>
              <option value="ASIA">ASIA</option>
              <option value="OCE">OCE</option>
              <option value="ME">ME</option>
            </select>
          </label>
          <label class="sp-control sp-control--wide">
            <span>Tournament and round</span>
            <select id="sp-window" disabled>
              <option>Loading tournament catalog…</option>
            </select>
          </label>
          <div class="sp-control sp-control--view">
            <span>Board size</span>
            <div class="sp-segmented" role="group" aria-label="Board size">
              <button id="sp-top5" data-limit="5" class="is-active" type="button" aria-pressed="true">Top 5</button>
              <button id="sp-top10" data-limit="10" type="button" aria-pressed="false">Top 10</button>
            </div>
          </div>
        </div>

        <div class="sp-status-line" role="status" aria-live="polite">
          <span class="sp-status-dot" id="sp-status-dot"></span>
          <span id="sp-status">Loading tournament catalog…</span>
          <span class="sp-status-delay" id="sp-delay"></span>
        </div>

        <div class="sp-summary" aria-label="Leaderboard summary">
          <div class="sp-summary-item sp-summary-item--live"><span class="sp-summary-label">Signal</span><strong id="sp-signal">Waiting</strong></div>
          <div class="sp-summary-item"><span class="sp-summary-label" title="Team formats count a team as one Epic leaderboard entry">Players shown / total</span><strong id="sp-count">—</strong></div>
          <div class="sp-summary-item"><span class="sp-summary-label">Last provider update</span><strong id="sp-updated">—</strong></div>
          <div class="sp-summary-item"><span class="sp-summary-label">Games reported</span><strong id="sp-games">—</strong></div>
          <div class="sp-summary-item sp-summary-item--prize"><span class="sp-summary-label" id="sp-prize-label">Prize pool</span><strong id="sp-prizepool" class="is-unpublished">Not published</strong><a id="sp-prize-source" class="sp-prize-source" href="#" target="_blank" rel="noreferrer" hidden>Official source ↗</a></div>
          <div class="sp-summary-item sp-summary-item--source"><span class="sp-summary-label">Source</span><strong id="sp-source">Epic leaderboard</strong></div>
        </div>

        <details class="sp-rules" id="sp-rules">
          <summary>
            <span class="sp-rules-title">Format &amp; scoring</span>
            <span class="sp-rules-summary" id="sp-rules-summary">Provider details</span>
            <span class="sp-rules-chevron" aria-hidden="true">⌄</span>
          </summary>
          <div class="sp-rules-grid">
            <div class="sp-rule"><span>Format</span><strong id="sp-rule-format">Not published</strong></div>
            <div class="sp-rule"><span>Kill points</span><strong id="sp-rule-kills">Not published</strong></div>
            <div class="sp-rule"><span>Placement points</span><strong id="sp-rule-placement">Not published</strong></div>
            <div class="sp-rule"><span>Payout model</span><strong id="sp-rule-payout">Not published</strong></div>
          </div>
          <p class="sp-rules-note" id="sp-rules-note">The provider has not published format or scoring details for this round.</p>
        </details>

        <div class="sp-board" id="sp-board" aria-live="polite">
          <div class="sp-board-head" aria-hidden="true">
            <span>Move</span><span>Competitor</span><span>Points</span><span>Wins</span><span>Prize</span><span>Next target</span>
          </div>
          <div class="sp-rows" id="sp-rows"></div>
          <div class="sp-empty" id="sp-empty">
            <span class="sp-empty-icon" aria-hidden="true">◎</span>
            <strong id="sp-empty-title">Select a tournament to load the board.</strong>
            <span id="sp-empty-detail">When the provider has processed a replay, the first rows will appear here.</span>
            <div class="sp-future" id="sp-future" hidden>
              <div class="sp-future-heading"><span>UP NEXT</span><span id="sp-future-region">Provider schedule</span></div>
              <div class="sp-future-list" id="sp-future-list"></div>
            </div>
          </div>
        </div>

        <div class="sp-footnote">
          <span class="sp-footnote-mark">i</span>
          <p><strong>Why “live-ish”?</strong> Epic replay and session processing can trail a live event by several minutes. This board shows the last successful provider snapshot and never invents a score while an update is missing.</p>
        </div>
      </section>

      <section class="sp-lower-grid" aria-label="How to use the board">
        <article class="sp-info-card sp-info-card--gold">
          <span class="sp-info-number">01</span>
          <div><p class="sp-section-label">For the desk</p><h2>Catch the comeback.</h2><p>Every row carries the one detail a caster needs next: places gained, points to the player ahead, and the last-game swing.</p></div>
        </article>
        <article class="sp-info-card sp-info-card--cyan">
          <span class="sp-info-number">02</span>
          <div><p class="sp-section-label">For the stream</p><h2>One board, two homes.</h2><p>Use this page for the watch party. In Streammaxxing, copy the Standings OBS URL and choose Top 5 or Top 10 for the on-stream version.</p></div>
        </article>
      </section>

      <footer class="sp-footer-line">
        <span>Built for the watch party</span>
        <a href="/streammaxxing">Open Streammaxxing <span aria-hidden="true">↗</span></a>
      </footer>
    </div>
  </div>
</Layout>

<script define:vars={{ standingsApi }}>
  const API = standingsApi;
  const REGIONS = new Set(['ALL', 'NAC', 'NAW', 'EU', 'BR', 'ASIA', 'OCE', 'ME']);
  const $ = (id) => document.getElementById(id);
  const state = {
    region: 'ALL',
    eventId: '',
    windowId: '',
    windows: [],
    limit: 5,
    pollSeconds: 90,
    timer: null,
    request: 0,
    catalogRequest: 0,
    standingsController: null,
    catalogController: null,
    hasRows: false,
    previousRows: new Map(),
    selectedWindow: null,
    overlay: false,
    collapsed: false,
  };

  function setStatus(message, tone = 'idle') {
    $('sp-status').textContent = message;
    $('sp-status-dot').className = `sp-status-dot sp-status-dot--${tone}`;
  }

  function formatNumber(value) {
    return Number(value || 0).toLocaleString('en-US');
  }

  function formatSigned(value) {
    const number = Number(value);
    if (!Number.isFinite(number) || number === 0) return '0';
    return `${number > 0 ? '+' : '−'}${formatNumber(Math.abs(number))}`;
  }

  function formatTime(value) {
    const date = new Date(value);
    if (!Number.isFinite(date.getTime())) return '—';
    return date.toLocaleTimeString([], { hour: 'numeric', minute: '2-digit' });
  }

  const PACIFIC_TIME_ZONE = 'America/Los_Angeles';

  function formatPacificTime(value) {
    const date = new Date(value);
    if (!Number.isFinite(date.getTime())) return '—';
    return new Intl.DateTimeFormat('en-US', {
      timeZone: PACIFIC_TIME_ZONE,
      hour: 'numeric',
      minute: '2-digit',
      timeZoneName: 'short',
    }).format(date);
  }

  function formatDateTime(value) {
    const date = new Date(value);
    if (!Number.isFinite(date.getTime())) return 'Time not published';
    return date.toLocaleString([], { month: 'short', day: 'numeric', hour: 'numeric', minute: '2-digit' });
  }

  function firstPublished(...values) {
    for (const value of values) {
      if (value !== null && value !== undefined && value !== '') return value;
    }
    return null;
  }

  function readPublishedField(sources, keys) {
    for (const source of sources) {
      if (!source || typeof source !== 'object' || Array.isArray(source)) continue;
      for (const key of keys) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          const value = source[key];
          if (value !== null && value !== undefined && value !== '') return value;
        }
      }
    }
    return null;
  }

  function providerSources(data = {}, item = {}) {
    return [
      data?.scoring,
      data?.rules,
      data?.payouts,
      item?.scoring,
      item?.rules,
      item?.payouts,
      data,
      item,
    ].filter(Boolean);
  }

  function formatPhase(item = {}, data = {}) {
    const explicit = firstPublished(
      item.roundType,
      item.phase,
      item.stage,
      item.format,
      item.competitionFormat,
      data.roundType,
      data.phase,
      data.stage,
      data.format,
      data.competitionFormat,
    );
    if (typeof explicit === 'string' && explicit.trim()) return explicit.trim();
    if (explicit && typeof explicit === 'object') {
      const label = firstPublished(explicit.label, explicit.name, explicit.title, explicit.type);
      if (label) return String(label);
    }
    const searchable = [item.name, item.windowName, item.windowId, item.eventName, item.eventId]
      .filter(Boolean)
      .join(' ')
      .toLowerCase();
    if (/\bsemi[- ]?finals?\b/.test(searchable)) return 'Semifinals';
    if (/\bfinals?\b/.test(searchable)) return 'Finals';
    if (/\bopens?\b/.test(searchable)) return 'Opens';
    if (/\bqualif/.test(searchable)) return 'Qualifiers';
    return 'Format not published';
  }

  function formatSchedule(item = {}) {
    const begin = item.beginTime || item.startTime || item.startsAt;
    const end = item.endTime || item.finishTime || item.endsAt;
    if (!begin && !end) return 'Time not published';
    if (!begin) return `Ends ${formatDateTime(end)}`;
    if (!end) return formatDateTime(begin);
    const beginDate = new Date(begin);
    const endDate = new Date(end);
    if (Number.isFinite(beginDate.getTime()) && Number.isFinite(endDate.getTime()) && beginDate.toDateString() === endDate.toDateString()) {
      return `${formatDateTime(begin)} – ${formatTime(end)}`;
    }
    return `${formatDateTime(begin)} – ${formatDateTime(end)}`;
  }

  function isUpcoming(item = {}) {
    if (item.status === 'upcoming' || item.upcoming === true) return true;
    if (item.live === true || item.status === 'live') return false;
    const begin = new Date(item.beginTime || item.startTime || item.startsAt);
    return Number.isFinite(begin.getTime()) && begin.getTime() > Date.now();
  }

  function selectedWindow() {
    return state.windows.find((item) => item.eventId === state.eventId && item.windowId === state.windowId) || state.selectedWindow || null;
  }

  function renderEventContext(item = null, data = {}) {
    const event = item || {};
    $('sp-board-event').textContent = event.name || event.eventName || event.eventId || 'Tournament';
    const round = event.windowName || event.roundName || event.windowId || data.windowId || 'Round';
    const phase = formatPhase(event, data);
    const hasPhase = phase !== 'Format not published' && !String(round).toLowerCase().includes(phase.toLowerCase());
    $('sp-board-round').textContent = hasPhase ? `${round} · ${phase}` : round;
  }

  function updateUrl() {
    const url = new URL(window.location.href);
    url.searchParams.set('region', state.region);
    if (state.eventId) url.searchParams.set('eventId', state.eventId); else url.searchParams.delete('eventId');
    if (state.windowId) url.searchParams.set('windowId', state.windowId); else url.searchParams.delete('windowId');
    url.searchParams.set('limit', String(state.limit));
    if (state.overlay) url.searchParams.set('overlay', '1'); else url.searchParams.delete('overlay');
    window.history.replaceState({}, '', url);
  }

  function overlayUrl() {
    const url = new URL(window.location.href);
    url.searchParams.set('overlay', '1');
    url.searchParams.set('limit', String(state.limit));
    return url.toString();
  }

  function updateFullBoardLink() {
    const url = new URL(window.location.href);
    url.searchParams.delete('overlay');
    url.searchParams.set('limit', String(state.limit));
    $('sp-full-board').href = url.pathname + url.search;
  }

  function renderFutureWindows() {
    const future = $('sp-future');
    const list = $('sp-future-list');
    if (!future || !list) return;
    list.replaceChildren();
    const upcoming = state.windows.filter(isUpcoming).slice(0, 4);
    future.hidden = upcoming.length === 0;
    if (!upcoming.length) return;

    $('sp-future-region').textContent = state.region === 'ALL' ? 'All regions' : state.region;
    upcoming.forEach((item) => {
      const button = document.createElement('button');
      button.type = 'button';
      button.className = 'sp-future-item';
      button.dataset.eventId = item.eventId || '';
      button.dataset.windowId = item.windowId || '';

      const copy = document.createElement('span');
      copy.className = 'sp-future-copy';
      const name = document.createElement('strong');
      name.textContent = item.name || item.eventName || item.eventId || 'Unnamed tournament';
      const round = document.createElement('small');
      const roundLabel = item.windowName || item.roundName || item.windowId || 'Round';
      const phase = formatPhase(item);
      round.textContent = phase !== 'Format not published' && !String(roundLabel).toLowerCase().includes(phase.toLowerCase()) ? `${roundLabel} · ${phase}` : roundLabel;
      copy.append(name, round);

      const schedule = document.createElement('span');
      schedule.className = 'sp-future-schedule';
      schedule.textContent = formatSchedule(item);

      const arrow = document.createElement('span');
      arrow.className = 'sp-future-arrow';
      arrow.setAttribute('aria-hidden', 'true');
      arrow.textContent = '↗';
      button.append(copy, schedule, arrow);
      button.setAttribute('aria-label', `Load ${name.textContent}, ${round.textContent}, ${schedule.textContent}`);
      list.append(button);
    });
  }

  function renderEmptyState(mode = 'default') {
    const title = $('sp-empty-title');
    const detail = $('sp-empty-detail');
    if (!title || !detail) return;
    const item = selectedWindow();
    const status = item?.status;
    if (mode === 'loading') {
      title.textContent = 'Loading provider snapshot…';
      detail.textContent = 'Fetching the selected tournament and round.';
    } else if (mode === 'no-selection') {
      title.textContent = 'Select a tournament round';
      detail.textContent = 'Choose a round above to load its standings.';
    } else if (mode === 'standings-error') {
      title.textContent = 'Standings unavailable';
      detail.textContent = 'The provider did not return a snapshot for this round. Try refreshing when it is reachable.';
    } else if (mode === 'catalog-error') {
      title.textContent = 'Tournament catalog unavailable';
      detail.textContent = 'The provider did not return a schedule. Try refreshing when it is reachable.';
    } else if (mode === 'no-catalog') {
      title.textContent = 'No tournament rounds available';
      detail.textContent = `There are no active rounds for ${state.region === 'ALL' ? 'the selected regions' : state.region} right now.`;
    } else if (mode === 'no-rows') {
      title.textContent = status === 'upcoming' ? 'This round has not started' : 'Waiting for provider snapshot';
      detail.textContent = status === 'upcoming'
        ? 'The next round is scheduled below. Standings will appear after the provider processes a replay.'
        : 'No processed standings were returned for this round yet.';
    } else {
      title.textContent = 'Select a tournament to load the board.';
      detail.textContent = 'When the provider has processed a replay, the first rows will appear here.';
    }
    renderFutureWindows();
  }

  function windowLabel(item) {
    const flags = [];
    if (item.live) flags.push('LIVE');
    else if (item.status === 'upcoming') flags.push('NEXT');
    else flags.push('RECENT');
    const name = item.name || item.eventName || item.eventId || 'Unnamed tournament';
    const round = item.windowName || item.roundName || item.windowId || 'Round';
    const phase = formatPhase(item);
    const region = item.region ? ` · ${item.region}` : '';
    const roundLabel = phase !== 'Format not published' && !String(round).toLowerCase().includes(phase.toLowerCase()) ? `${round} · ${phase}` : round;
    return `${flags[0]} · ${name} · ${roundLabel}${region}`;
  }

  function renderWindows() {
    const select = $('sp-window');
    select.replaceChildren();
    if (!state.windows.length) {
      select.add(new Option('No tournaments returned for this region', ''));
      select.disabled = true;
      state.eventId = '';
      state.windowId = '';
      state.selectedWindow = null;
      renderEventContext(null);
      renderRules(null, {});
      renderFutureWindows();
      updateUrl();
      return;
    }
    state.windows.forEach((item) => select.add(new Option(windowLabel(item), `${item.eventId}::${item.windowId}`)));
    const preferred = state.windows.find((item) => item.eventId === state.eventId && item.windowId === state.windowId);
    const selected = preferred || state.windows[0];
    state.eventId = selected.eventId || '';
    state.windowId = selected.windowId || '';
    state.selectedWindow = selected;
    select.value = `${state.eventId}::${state.windowId}`;
    select.disabled = false;
    renderEventContext(selected);
    renderRules(selected, {});
    renderFutureWindows();
    updateUrl();
  }

  function renderLegacyRows(rows) {
    const container = $('sp-rows');
    container.replaceChildren();
    const visible = Array.isArray(rows) ? rows.slice(0, state.limit) : [];
    $('sp-count').textContent = visible.length ? `${visible.length} / ${state.limit}` : '—';
    state.hasRows = visible.length > 0;
    $('sp-empty').hidden = state.hasRows;
    if (!visible.length) return;

    visible.forEach((row, index) => {
      const rank = Number(row.rank) || index + 1;
      const delta = Number(row.rankDelta);
      const movement = Number.isFinite(delta) && delta !== 0;
      const article = document.createElement('article');
      const rankClass = rank <= 3 ? `sp-row--rank-${rank}` : 'sp-row--standard';
      article.className = `sp-row ${rankClass} ${movement ? (delta > 0 ? 'sp-row--up' : 'sp-row--down') : 'sp-row--steady'}`;
      article.style.setProperty('--row-delay', `${Math.min(index, 9) * 55}ms`);

      const move = document.createElement('div');
      move.className = 'sp-movement';
      move.setAttribute('aria-label', movement ? `${Math.abs(delta)} place${Math.abs(delta) === 1 ? '' : 's'} ${delta > 0 ? 'up' : 'down'}` : 'No movement');
      if (movement) {
        move.innerHTML = `<span class="sp-movement-arrow" aria-hidden="true">${delta > 0 ? '↑' : '↓'}</span><strong>${Math.abs(delta)}</strong>`;
      } else {
        move.innerHTML = '<span class="sp-movement-blank" aria-hidden="true"></span>';
      }

      const identity = document.createElement('div');
      identity.className = 'sp-player';
      const rankBadge = document.createElement('span');
      rankBadge.className = 'sp-rank';
      rankBadge.textContent = `#${rank}`;
      const name = document.createElement('strong');
      name.textContent = row.name || 'Player unavailable';
      identity.append(rankBadge, name);

      const points = document.createElement('div');
      points.className = 'sp-points';
      const pointsValue = document.createElement('strong');
      pointsValue.textContent = formatNumber(row.points);
      const pointsLabel = document.createElement('span');
      pointsLabel.textContent = 'PTS';
      points.append(pointsValue, pointsLabel);

      const math = document.createElement('div');
      math.className = 'sp-math';
      const gap = document.createElement('span');
      gap.className = 'sp-gap';
      gap.textContent = row.pointsToNext == null ? '— lead' : `+${formatNumber(row.pointsToNext)} to #${formatNumber(row.nextRank || Math.max(1, rank - 1))}`;
      const last = document.createElement('span');
      const lastKnown = row.lastGamePoints != null && Number.isFinite(Number(row.lastGamePoints));
      last.className = `sp-last ${lastKnown && Number(row.lastGamePoints) < 0 ? 'sp-last--negative' : ''}`;
      last.textContent = lastKnown ? `${formatSigned(row.lastGamePoints)} points · last game` : 'LAST GAME —';
      last.title = lastKnown ? `${formatSigned(row.lastGamePoints)} points scored in the last game` : 'Last-game points not published';
      math.append(gap, last);

      article.append(move, identity, points, math);
      container.append(article);
    });
  }

  function formatNullableNumber(value) {
    if (value === null || value === undefined || value === '') return '—';
    const number = Number(value);
    return Number.isFinite(number) ? number.toLocaleString('en-US') : '—';
  }

  function formatPrize(value, currency = 'USD') {
    const number = numericAmount(value);
    if (number === null) return '—';
    const symbol = currency === 'EUR' ? '€' : currency === 'GBP' ? '£' : '$';
    return symbol + number.toLocaleString('en-US', { maximumFractionDigits: 0 });
  }

  function numericAmount(value) {
    if (value === null || value === undefined || value === '') return null;
    if (value && typeof value === 'object') {
      return numericAmount(value.amount ?? value.value ?? value.total);
    }
    if (typeof value === 'string') value = value.replace(/[$,]/g, '');
    const number = Number(value);
    return Number.isFinite(number) ? number : null;
  }

  function prizePoolAmount(data) {
    const candidates = [data?.prizePool, data?.prizepool, data?.totalPrizePool, data?.payoutTotal, data?.payouts?.total];
    for (const candidate of candidates) {
      const amount = numericAmount(candidate);
      if (amount !== null) return amount;
    }
    return null;
  }

  function explicitPerWinAmount(data = {}, item = {}) {
    const value = readPublishedField(providerSources(data, item), ['perWin', 'payoutPerWin', 'rewardPerWin', 'moneyPerWin', 'winPayout', 'payoutPerVictory']);
    return numericAmount(value);
  }

  function renderPrizePool(data, fallback = 'Not published', item = selectedWindow()) {
    const element = $('sp-prizepool');
    if (!element) return;
    const pool = prizePoolAmount(data);
    const perWin = pool === null ? explicitPerWinAmount(data, item || {}) : null;
    const announced = typeof data?.prizeDisplay === 'string' && data.prizeDisplay.trim()
      ? data.prizeDisplay.trim()
      : String(data?.prizeStatus || '').toLowerCase() === 'announced'
        ? 'Amount pending'
        : '';
    const published = pool !== null || perWin !== null;
    $('sp-prize-label').textContent = data?.prizeLabel || (perWin !== null ? 'Payout per win' : 'Prize pool');
    element.textContent = pool !== null
      ? formatPrize(pool, data?.prizeCurrency)
      : perWin !== null
        ? `${formatPrize(perWin, data?.prizeCurrency)} / win`
        : announced || fallback;
    element.classList.toggle('is-published', published);
    element.classList.toggle('is-unpublished', !published);
    element.classList.toggle('is-announced', Boolean(announced) && !published);
    const sourceLink = $('sp-prize-source');
    const sourceUrl = typeof data?.prizeSourceUrl === 'string' && /^https:\/\//i.test(data.prizeSourceUrl)
      ? data.prizeSourceUrl
      : '';
    if (sourceLink) {
      sourceLink.hidden = !sourceUrl;
      if (sourceUrl) sourceLink.href = sourceUrl;
    }
  }

  function ruleValue(value, kind = '') {
    if (value === null || value === undefined || value === '') return 'Not published';
    if (typeof value === 'object') {
      const nested = kind === 'kills'
        ? readPublishedField([value], ['perKill', 'perElimination', 'pointsPerKill', 'pointsPerElimination'])
        : kind === 'placement'
          ? readPublishedField([value], ['points', 'pointsByPlacement', 'table', 'values'])
          : null;
      if (nested !== null && nested !== value) return ruleValue(nested, kind);
      const label = firstPublished(value.label, value.name, value.title, value.description, value.type);
      if (label) return String(label);
      return 'Published';
    }
    if (typeof value === 'boolean') return value ? 'Published' : 'Not published';
    if (typeof value === 'number' && Number.isFinite(value)) {
      if (kind === 'kills') return `${formatNumber(value)} pts / elimination`;
      if (kind === 'placement') return `${formatNumber(value)} pts`;
      return formatNumber(value);
    }
    return String(value);
  }

  function renderRules(item = null, data = {}) {
    const event = item || {};
    const sources = providerSources(data, event);
    const format = formatPhase(event, data);
    const kills = readPublishedField(sources, ['killPoints', 'eliminationPoints', 'pointsPerKill', 'pointsPerElimination', 'elimPoints', 'kills']);
    const placement = readPublishedField(sources, ['placementPoints', 'pointsForPlacement', 'placementScoring', 'placement']);
    const payoutModel = readPublishedField(sources, ['payoutModel', 'prizeModel', 'payoutType', 'prizeType', 'rewardType']);
    const perWin = explicitPerWinAmount(data, event);
    const payout = perWin !== null ? `${formatPrize(perWin)} / win` : ruleValue(payoutModel, 'payout');
    const scoringPublished = kills !== null || placement !== null;
    const payoutPublished = perWin !== null || payoutModel !== null;
    $('sp-rule-format').textContent = format;
    $('sp-rule-kills').textContent = ruleValue(kills, 'kills');
    $('sp-rule-placement').textContent = ruleValue(placement, 'placement');
    $('sp-rule-payout').textContent = payout;
    $('sp-rules-summary').textContent = `${format} · ${scoringPublished || payoutPublished ? 'metadata published' : 'details not published'}`;
    const source = data.source || 'The provider';
    $('sp-rules-note').textContent = scoringPublished || payoutPublished
      ? `Only format, scoring, and payout fields explicitly returned by ${source} are shown here.`
      : item || data.eventId
        ? `${source} has not published scoring or payout details for this round.`
        : 'Select a tournament round to check its published format and scoring details.';
  }

  function rankBadge(rank) {
    if (rank === 1) {
      return '<svg class="sp-trophy" viewBox="0 0 24 24" aria-hidden="true"><path d="M7 4h10v4.6c0 3.02-2.1 5.28-5 5.28S7 11.62 7 8.6V4Z"/><path d="M7 6H3.8v2.1c0 2.1 1.52 3.7 3.55 3.7M17 6h3.2v2.1c0 2.1-1.52 3.7-3.55 3.7M12 13.9V18M8.5 20h7"/></svg>';
    }
    if (rank === 2) return '<span class="sp-rank-roman">II</span>';
    if (rank === 3) return '<span class="sp-rank-roman">III</span>';
    return '<span class="sp-rank-number">#' + rank + '</span>';
  }

  function animateStandingsNumber(element, from, to) {
    const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (!element || !Number.isFinite(from) || !Number.isFinite(to) || from === to || reduceMotion) {
      if (element) element.textContent = formatNumber(to);
      return;
    }
    const started = performance.now();
    const duration = 720;
    const frame = (now) => {
      const progress = Math.min(1, (now - started) / duration);
      const eased = 1 - Math.pow(1 - progress, 3);
      element.textContent = formatNumber(Math.round(from + (to - from) * eased));
      if (progress < 1) window.requestAnimationFrame(frame);
    };
    window.requestAnimationFrame(frame);
  }

  function renderRows(rows, totalPlayers = null) {
    const container = $('sp-rows');
    container.replaceChildren();
    const visible = Array.isArray(rows) ? rows.slice(0, state.limit) : [];
    const providerTotal = Number(totalPlayers);
    const totalLabel = Number.isFinite(providerTotal) && providerTotal > 0
      ? formatNumber(providerTotal)
      : '—';
    $('sp-count').textContent = visible.length ? visible.length + ' / ' + totalLabel : '0 / ' + totalLabel;
    state.hasRows = visible.length > 0;
    $('sp-empty').hidden = state.hasRows;
    if (!visible.length) {
      state.previousRows = new Map();
      return;
    }

    const nextRows = new Map();
    visible.forEach((row, index) => {
      const rank = Number(row.rank) || index + 1;
      const delta = Number(row.rankDelta);
      const movement = Number.isFinite(delta) && delta !== 0;
      const rowKey = row.name || 'rank-' + rank;
      const prior = state.previousRows.get(rowKey);
      const article = document.createElement('article');
      const rankClass = rank <= 3 ? 'sp-row--rank-' + rank : 'sp-row--standard';
      article.className = 'sp-row ' + rankClass + ' ' + (movement ? (delta > 0 ? 'sp-row--up' : 'sp-row--down') : 'sp-row--steady');
      article.style.setProperty('--row-delay', Math.min(index, 9) * 48 + 'ms');
      article.dataset.rank = String(rank);

      const move = document.createElement('div');
      move.className = 'sp-movement';
      move.setAttribute('aria-label', movement ? Math.abs(delta) + ' place' + (Math.abs(delta) === 1 ? '' : 's') + ' ' + (delta > 0 ? 'up' : 'down') : 'No movement');
      move.innerHTML = movement
        ? '<span class="sp-movement-arrow" aria-hidden="true">' + (delta > 0 ? '↑' : '↓') + '</span><strong>' + Math.abs(delta) + '</strong>'
        : '<span class="sp-movement-blank" aria-hidden="true"></span>';

      const identity = document.createElement('div');
      identity.className = 'sp-player';
      const rankBadgeElement = document.createElement('span');
      rankBadgeElement.className = 'sp-rank';
      rankBadgeElement.innerHTML = rankBadge(rank);
      const identityCopy = document.createElement('span');
      identityCopy.className = 'sp-player-copy';
      const name = document.createElement('strong');
      name.textContent = row.name || 'Player unavailable';
      const meta = document.createElement('small');
      const selected = state.windows.find((item) => item.eventId === state.eventId && item.windowId === state.windowId);
      const region = row.region || (selected && selected.region) || (state.region !== 'ALL' ? state.region : '');
      meta.textContent = region ? region + ' · ' + formatNullableNumber(row.games) + ' games' : formatNullableNumber(row.games) + ' games';
      identityCopy.append(name, meta);
      identity.append(rankBadgeElement, identityCopy);

      const points = document.createElement('div');
      points.className = 'sp-points';
      const pointsValue = document.createElement('strong');
      const currentPoints = Number(row.points);
      pointsValue.textContent = formatNullableNumber(row.points);
      const oldPoints = prior ? Number(prior.points) : NaN;
      if (Number.isFinite(oldPoints) && Number.isFinite(currentPoints) && oldPoints !== currentPoints) {
        pointsValue.classList.add(currentPoints > oldPoints ? 'sp-points-flash' : 'sp-points-flash-red');
        animateStandingsNumber(pointsValue, oldPoints, currentPoints);
      }
      const pointsLabel = document.createElement('span');
      pointsLabel.textContent = 'PTS';
      points.append(pointsValue, pointsLabel);

      const wins = document.createElement('div');
      wins.className = 'sp-wins';
      wins.innerHTML = '<strong>' + formatNullableNumber(row.wins) + '</strong><span>WINS</span>';

      const prize = document.createElement('div');
      prize.className = 'sp-prize ' + (rank <= 3 ? 'sp-prize--tier-' + rank : 'sp-prize--standard');
      const prizeKnown = numericAmount(row.prize) !== null;
      prize.innerHTML = '<strong>' + formatPrize(row.prize) + '</strong><span>' + (prizeKnown ? 'PUBLISHED PAYOUT' : 'PRIZE DATA PENDING') + '</span>';

      const math = document.createElement('div');
      math.className = 'sp-math';
      const gap = document.createElement('strong');
      gap.className = 'sp-gap';
      if (rank === 1) {
        gap.textContent = 'LEAD';
        gap.title = 'This player is currently leading the displayed standings.';
        gap.setAttribute('aria-label', gap.title);
      } else if (row.pointsToNext != null && Number.isFinite(Number(row.pointsToNext))) {
        const pointsToNext = Number(row.pointsToNext);
        const providerNextRank = Number(row.nextRank);
        const hasProviderNextRank = Number.isFinite(providerNextRank);
        const nextRank = hasProviderNextRank ? Math.max(1, providerNextRank) : Math.max(1, rank - 1);
        gap.textContent = (pointsToNext > 0 ? '+' : '') + formatNullableNumber(pointsToNext) + ' to #' + nextRank;
        gap.title = hasProviderNextRank
          ? 'Provider-reported gap: ' + formatNullableNumber(pointsToNext) + ' points to the next higher position (#' + nextRank + ').'
          : 'Provider-reported gap: ' + formatNullableNumber(pointsToNext) + ' points to the next higher position. Target #' + nextRank + ' follows the displayed board order.';
        gap.setAttribute('aria-label', gap.title);
      } else {
        gap.textContent = 'NEXT TARGET —';
        gap.title = 'The provider did not publish a points-to-next gap for this player.';
        gap.setAttribute('aria-label', gap.title);
      }
      const last = document.createElement('span');
      const lastKnown = row.lastGamePoints != null && Number.isFinite(Number(row.lastGamePoints));
      last.className = 'sp-last' + (lastKnown && Number(row.lastGamePoints) < 0 ? ' sp-last--negative' : '');
      last.textContent = lastKnown ? formatSigned(row.lastGamePoints) + ' points · last game' : 'LAST GAME —';
      last.title = lastKnown ? formatSigned(row.lastGamePoints) + ' points scored in the last game' : 'Last-game points not published';
      last.setAttribute('aria-label', last.title);
      math.append(gap, last);

      article.append(move, identity, points, wins, prize, math);
      container.append(article);
      nextRows.set(rowKey, { points: currentPoints });
    });
    state.previousRows = nextRows;
  }

  async function readJson(url, signal) {
    const response = await fetch(url, { signal, headers: { Accept: 'application/json' } });
    const data = await response.json().catch(() => ({}));
    if (!response.ok) throw new Error(data.error || `Provider returned HTTP ${response.status}`);
    return data;
  }

  function abortStandings() {
    if (state.standingsController) state.standingsController.abort();
    state.standingsController = null;
  }

  function abortCatalog() {
    if (state.catalogController) state.catalogController.abort();
    state.catalogController = null;
  }

  async function loadStandings({ quiet = false, reset = false } = {}) {
    window.clearTimeout(state.timer);
    const requestId = ++state.request;
    abortStandings();
    if (!state.eventId || !state.windowId) {
      renderRows([]);
      renderEmptyState('no-selection');
      renderRules(null, {});
      renderPrizePool({}, 'Not published', null);
      $('sp-signal').textContent = 'Choose a round';
      setStatus('Choose a tournament round to load standings.', 'idle');
      return;
    }
    if (reset) {
      renderRows([]);
      renderEmptyState('loading');
    } else if (!state.hasRows) {
      renderEmptyState('loading');
    }
    const item = selectedWindow();
    const controller = new AbortController();
    state.standingsController = controller;
    let timedOut = false;
    const timeoutHandler = window.setTimeout(() => {
      timedOut = true;
      controller.abort();
    }, 12000);
    if (reset || !quiet || !state.hasRows) renderPrizePool({}, 'Checking…', item);
    if (!quiet || reset) {
      const round = item?.windowName || item?.roundName || item?.windowId || 'selected round';
      setStatus(`Loading ${round}…`, 'loading');
    }
    try {
      const url = new URL(`${API}/standings`, window.location.origin);
      url.searchParams.set('eventId', state.eventId);
      url.searchParams.set('windowId', state.windowId);
      url.searchParams.set('limit', '10');
      const data = await readJson(url, controller.signal);
      if (requestId !== state.request) return;
      renderRows(data.rows, data.totalPlayers ?? data.totalEntries ?? data.totalTeams);
      renderRules(item, data);
      renderPrizePool(data, 'Not published', item);
      if (!state.hasRows) renderEmptyState('no-rows');
      state.pollSeconds = Math.min(300, Math.max(60, Number(data.pollAfterSeconds) || 90));
      $('sp-signal').textContent = state.hasRows ? 'Provider snapshot' : 'No processed rows';
      $('sp-updated').textContent = formatPacificTime(data.fetched);
      $('sp-source').textContent = data.source || 'Epic leaderboard';
      $('sp-games').textContent = Number.isFinite(Number(data.maxGames)) ? Number(data.maxGames).toLocaleString('en-US') : '—';
      $('sp-delay').textContent = data.replayAware ? 'PROCESSING DELAY EXPECTED' : '';
      $('sp-board-status').textContent = state.hasRows ? `UPDATED ${formatPacificTime(data.fetched)}` : 'NO PROCESSED ROWS';
      setStatus(`Updated ${formatPacificTime(data.fetched)} · next check in ${state.pollSeconds}s`, 'good');
      updateUrl();
      schedulePoll();
    } catch (error) {
      if (requestId !== state.request) return;
      if (error.name === 'AbortError' && !timedOut) return;
      $('sp-signal').textContent = state.hasRows ? 'Showing last good' : 'Unavailable';
      if (!state.hasRows) {
        renderPrizePool({}, 'Unavailable', item);
        renderEmptyState('standings-error');
      }
      $('sp-board-status').textContent = state.hasRows ? 'LAST GOOD SNAPSHOT' : 'DATA UNAVAILABLE';
      setStatus(state.hasRows ? `Provider update unavailable — showing the last good snapshot. ${error.message}` : `Standings unavailable: ${error.message}`, 'warn');
      schedulePoll();
    } finally {
      window.clearTimeout(timeoutHandler);
      if (state.standingsController === controller) state.standingsController = null;
    }
  }

  async function loadWindows() {
    state.eventId = new URLSearchParams(window.location.search).get('eventId') || state.eventId;
    state.windowId = new URLSearchParams(window.location.search).get('windowId') || state.windowId;
    const requestId = ++state.catalogRequest;
    abortCatalog();
    state.request += 1;
    abortStandings();
    window.clearTimeout(state.timer);
    $('sp-window').disabled = true;
    renderRows([]);
    renderEmptyState('loading');
    setStatus(`Loading ${state.region === 'ALL' ? 'all regions' : state.region} tournaments…`, 'loading');
    const controller = new AbortController();
    state.catalogController = controller;
    let timedOut = false;
    const timeout = window.setTimeout(() => {
      timedOut = true;
      controller.abort();
    }, 12000);
    try {
      const url = new URL(`${API}/tournaments`, window.location.origin);
      url.searchParams.set('region', state.region);
      url.searchParams.set('history', '1');
      const data = await readJson(url, controller.signal);
      if (requestId !== state.catalogRequest) return;
      state.windows = Array.isArray(data.windows) ? data.windows : [];
      renderWindows();
      if (state.windows.length) {
        setStatus(`${state.windows.length} tournament rounds available · loading standings…`, 'loading');
        await loadStandings({ reset: true });
      } else {
        renderRows([]);
        renderEmptyState('no-catalog');
        $('sp-signal').textContent = 'No active rounds';
        $('sp-board-status').textContent = 'NO TOURNAMENTS';
        setStatus('No tournament rounds were returned for this region.', 'warn');
      }
    } catch (error) {
      if (requestId !== state.catalogRequest) return;
      if (error.name === 'AbortError' && !timedOut) return;
      state.windows = [];
      renderWindows();
      renderRows([]);
      renderEmptyState('catalog-error');
      $('sp-signal').textContent = 'Catalog unavailable';
      setStatus(`Tournament catalog unavailable: ${error.message}`, 'warn');
    } finally {
      window.clearTimeout(timeout);
      if (state.catalogController === controller) state.catalogController = null;
    }
  }

  function schedulePoll() {
    window.clearTimeout(state.timer);
    state.timer = window.setTimeout(() => {
      if (document.visibilityState === 'visible') loadStandings({ quiet: true });
      else schedulePoll();
    }, state.pollSeconds * 1000);
  }

  function setLimit(limit) {
    state.limit = limit === 10 ? 10 : 5;
    document.querySelectorAll('[data-limit]').forEach((button) => {
      const active = Number(button.dataset.limit) === state.limit;
      button.classList.toggle('is-active', active);
      button.setAttribute('aria-pressed', String(active));
    });
    updateUrl();
    updateFullBoardLink();
    if (state.eventId && state.windowId) loadStandings({ quiet: true });
  }

  $('sp-region').addEventListener('change', (event) => {
    state.region = REGIONS.has(event.target.value) ? event.target.value : 'ALL';
    state.eventId = '';
    state.windowId = '';
    state.windows = [];
    state.selectedWindow = null;
    abortStandings();
    window.clearTimeout(state.timer);
    updateUrl();
    renderRows([]);
    renderEmptyState('loading');
    loadWindows();
  });
  $('sp-window').addEventListener('change', (event) => {
    const [eventId, windowId] = String(event.target.value || '').split('::');
    state.eventId = eventId || '';
    state.windowId = windowId || '';
    state.selectedWindow = selectedWindow();
    renderEventContext(state.selectedWindow);
    renderRules(state.selectedWindow, {});
    updateUrl();
    loadStandings({ reset: true });
  });
  document.querySelectorAll('[data-limit]').forEach((button) => {
    button.addEventListener('click', () => setLimit(Number(button.dataset.limit)));
  });
  $('sp-refresh').addEventListener('click', () => loadStandings());
  $('sp-future-list').addEventListener('click', (event) => {
    const button = event.target.closest('button[data-event-id][data-window-id]');
    if (!button) return;
    state.eventId = button.dataset.eventId || '';
    state.windowId = button.dataset.windowId || '';
    state.selectedWindow = selectedWindow();
    const select = $('sp-window');
    select.value = `${state.eventId}::${state.windowId}`;
    renderEventContext(state.selectedWindow);
    renderRules(state.selectedWindow, {});
    updateUrl();
    loadStandings({ reset: true });
  });
  $('sp-open-overlay').addEventListener('click', () => window.open(overlayUrl(), '_blank', 'noopener,noreferrer'));
  $('sp-copy-overlay').addEventListener('click', async () => {
    try {
      await navigator.clipboard.writeText(overlayUrl());
      setStatus('OBS URL copied. Add it as a Browser Source in OBS.', 'good');
    } catch {
      setStatus('Copy was blocked by the browser. Use the address bar URL for OBS.', 'idle');
    }
  });
  $('sp-overlay-collapse').addEventListener('click', () => {
    state.collapsed = !state.collapsed;
    document.querySelector('.standings-page').classList.toggle('sp-overlay-collapsed', state.collapsed);
    $('sp-overlay-collapse').setAttribute('aria-expanded', String(!state.collapsed));
    $('sp-overlay-collapse-label').textContent = state.collapsed ? 'Expand' : 'Minimize';
  });
  $('sp-share').addEventListener('click', async () => {
    updateUrl();
    try {
      await navigator.clipboard.writeText(window.location.href);
      setStatus('Board link copied. Share it with the watch party.', 'good');
    } catch {
      setStatus('The board is ready to share from the address bar.', 'idle');
    }
  });
  document.addEventListener('visibilitychange', () => {
    if (document.visibilityState === 'visible' && state.eventId) loadStandings({ quiet: true });
  });

  const params = new URLSearchParams(window.location.search);
  const initialRegion = String(params.get('region') || 'ALL').toUpperCase();
  state.region = REGIONS.has(initialRegion) ? initialRegion : 'ALL';
  state.limit = params.get('limit') === '10' ? 10 : 5;
  state.overlay = params.get('overlay') === '1' || params.get('view') === 'overlay';
  document.querySelector('.standings-page').classList.toggle('sp-overlay-mode', state.overlay);
  if (state.overlay) document.title = 'OBS Standings Overlay — Maxxtopia';
  $('sp-region').value = state.region;
  setLimit(state.limit);
  updateFullBoardLink();
  loadWindows();
</script>

<style is:global>
  .standings-page {
    position: relative;
    min-height: 100vh;
    overflow: hidden;
    background: #080613;
  }

  .sp-grid {
    position: absolute;
    inset: 0;
    opacity: .22;
    pointer-events: none;
    background-image: linear-gradient(rgba(255,255,255,.035) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,.035) 1px, transparent 1px);
    background-size: 54px 54px;
    mask-image: linear-gradient(to bottom, black, transparent 70%);
  }

  .sp-shell {
    position: relative;
    z-index: 1;
    width: min(1180px, calc(100% - 40px));
    margin: 0 auto;
    padding: 92px 0 48px;
  }

  .sp-hero { max-width: 940px; margin-bottom: 38px; }
  .sp-kicker, .sp-section-label, .sp-chip-top, .sp-chip-bottom, .sp-summary-label, .sp-board-head, .sp-footer-line, .sp-info-number {
    font-family: var(--font-mono);
    text-transform: uppercase;
    letter-spacing: .12em;
  }
  .sp-kicker { display: flex; align-items: center; gap: 9px; color: var(--color-cyan); font-size: 11px; font-weight: 600; }
  .sp-kicker-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--color-cyan); box-shadow: 0 0 18px rgba(0,212,255,.9); animation: sp-pulse 2.4s ease-in-out infinite; }
  .sp-title-row { display: flex; align-items: end; justify-content: space-between; gap: 30px; margin-top: 15px; }
  .sp-title-row h1 { margin: 0; color: var(--color-text); font-size: clamp(52px, 8vw, 112px); letter-spacing: -.065em; line-height: .84; font-weight: 800; }
  .sp-title-gradient { background: linear-gradient(102deg, #ffe2a1 4%, #ffb23e 34%, #e25bff 67%, #00d4ff 100%); -webkit-background-clip: text; background-clip: text; color: transparent; }
  .sp-title-mark { color: var(--color-warm); font-weight: 400; margin: 0 .08em; }
  .sp-title-muted { color: rgba(248,246,255,.32); font-size: .4em; font-family: var(--font-mono); letter-spacing: -.02em; vertical-align: .26em; }
  .sp-intro { max-width: 690px; margin: 24px 0 0; color: var(--color-text-mid); font-size: 16px; line-height: 1.65; }
  .sp-hero-chip { min-width: 172px; padding: 14px 15px 13px; border: 1px solid rgba(255,178,62,.36); border-radius: 9px; background: linear-gradient(145deg, rgba(255,178,62,.14), rgba(226,91,255,.08)); box-shadow: inset 0 1px 0 rgba(255,255,255,.06), 0 16px 40px rgba(0,0,0,.22); transform: rotate(2deg); }
  .sp-chip-top { display: block; color: var(--color-warm); font-size: 9px; }
  .sp-chip-main { display: block; margin-top: 4px; color: var(--color-text); font-family: var(--font-display); font-size: 25px; letter-spacing: .04em; line-height: .9; }
  .sp-chip-bottom { display: block; margin-top: 8px; color: var(--color-muted); font-size: 8px; letter-spacing: .05em; text-transform: none; }

  .sp-stage { border: 1px solid rgba(226,91,255,.22); border-radius: 18px; background: linear-gradient(140deg, rgba(22,12,46,.96), rgba(12,8,28,.97) 55%, rgba(13,19,39,.97)); box-shadow: 0 30px 90px rgba(0,0,0,.38), 0 0 0 1px rgba(255,255,255,.025) inset; overflow: hidden; }
  .sp-stage-topline { display: flex; align-items: end; justify-content: space-between; gap: 20px; padding: 28px 30px 22px; border-bottom: 1px solid rgba(255,255,255,.075); }
  .sp-section-label { margin: 0 0 7px; color: var(--color-magenta); font-size: 10px; font-weight: 600; }
  .sp-stage h2, .sp-info-card h2 { margin: 0; color: var(--color-text); font-size: clamp(23px, 3vw, 34px); letter-spacing: -.045em; }
  .sp-stage-actions { display: flex; gap: 9px; align-items: center; }
  .sp-quiet-button, .sp-refresh-button, .sp-segmented button { border: 1px solid rgba(255,255,255,.13); border-radius: 7px; background: rgba(255,255,255,.045); color: var(--color-text-mid); padding: 10px 13px; font: 600 11px var(--font-mono); letter-spacing: .03em; transition: background 160ms ease, border-color 160ms ease, transform 160ms ease; }
  .sp-refresh-button { border-color: rgba(0,212,255,.35); background: rgba(0,212,255,.1); color: #c9f8ff; }
  .sp-quiet-button:hover, .sp-refresh-button:hover, .sp-segmented button:hover { border-color: rgba(0,212,255,.6); background: rgba(0,212,255,.14); transform: translateY(-1px); }
  .sp-refresh-button span { font-size: 16px; vertical-align: -1px; }
  .sp-controls { display: grid; grid-template-columns: minmax(150px, .7fr) minmax(300px, 1.8fr) minmax(190px, .85fr); gap: 12px; padding: 22px 30px 14px; }
  .sp-control { display: flex; flex-direction: column; gap: 7px; color: var(--color-muted); font: 10px var(--font-mono); letter-spacing: .08em; text-transform: uppercase; }
  .sp-control select { width: 100%; min-height: 43px; border: 1px solid rgba(255,255,255,.12); border-radius: 7px; background: rgba(5,4,15,.76); color: var(--color-text); padding: 0 12px; font: 500 13px var(--font-sans); letter-spacing: 0; text-transform: none; }
  .sp-control select:disabled { color: var(--color-muted); opacity: .82; }
  .sp-segmented { display: flex; height: 43px; }
  .sp-segmented button { flex: 1; border-radius: 7px 0 0 7px; padding: 0 10px; color: var(--color-muted); }
  .sp-segmented button + button { border-left: 0; border-radius: 0 7px 7px 0; }
  .sp-segmented button.is-active { border-color: rgba(255,178,62,.55); background: rgba(255,178,62,.16); color: #ffe4ae; box-shadow: 0 0 22px rgba(255,178,62,.08) inset; }
  .sp-status-line { display: flex; align-items: center; gap: 9px; min-height: 26px; padding: 0 30px 17px; color: var(--color-muted); font-size: 12px; }
  .sp-status-dot { width: 7px; height: 7px; flex: none; border-radius: 50%; background: var(--color-muted); }
  .sp-status-dot--good { background: var(--color-success); box-shadow: 0 0 12px rgba(74,222,128,.7); }
  .sp-status-dot--loading { background: var(--color-cyan); animation: sp-pulse 1s ease-in-out infinite; }
  .sp-status-dot--warn { background: var(--color-warm); box-shadow: 0 0 12px rgba(255,178,62,.6); }
  .sp-summary { display: grid; grid-template-columns: 1fr 1fr 1.2fr 1.3fr; border-top: 1px solid rgba(255,255,255,.075); border-bottom: 1px solid rgba(255,255,255,.075); background: rgba(0,0,0,.16); }
  .sp-summary-item { min-height: 67px; display: flex; flex-direction: column; justify-content: center; gap: 6px; padding: 10px 20px; border-right: 1px solid rgba(255,255,255,.075); }
  .sp-summary-item:last-child { border-right: 0; }
  .sp-summary-label { color: var(--color-muted); font-size: 9px; }
  .sp-summary-item strong { color: var(--color-text); font: 600 12px var(--font-mono); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .sp-summary-item--live strong { color: var(--color-success); }
  .sp-board { padding: 0 30px 28px; }
  .sp-board-head { display: grid; grid-template-columns: 74px minmax(160px, 1fr) 130px minmax(160px, 220px); gap: 14px; padding: 18px 16px 10px; color: var(--color-subtle); font-size: 9px; }
  .sp-board-head span:nth-child(3), .sp-board-head span:nth-child(4) { text-align: right; }
  .sp-rows { display: flex; flex-direction: column; gap: 7px; }
  .sp-row { position: relative; display: grid; grid-template-columns: 74px minmax(160px, 1fr) 130px minmax(160px, 220px); align-items: center; gap: 14px; min-height: 75px; padding: 11px 16px; border: 1px solid rgba(255,255,255,.08); border-radius: 9px; background: rgba(255,255,255,.035); animation: sp-row-in 500ms cubic-bezier(.2,.8,.2,1) both; animation-delay: var(--row-delay); transition: transform 160ms ease, background 160ms ease, border-color 160ms ease; }
  .sp-row:hover { transform: translateX(3px); background: rgba(255,255,255,.06); border-color: rgba(255,255,255,.18); }
  .sp-row::before { position: absolute; inset: 0 auto 0 0; width: 3px; border-radius: 9px 0 0 9px; background: rgba(255,255,255,.25); content: ''; }
  .sp-row--rank-1 { border-color: rgba(255,194,77,.58); background: linear-gradient(100deg, rgba(255,178,62,.18), rgba(255,178,62,.06) 49%, rgba(255,255,255,.035)); box-shadow: 0 0 34px rgba(255,178,62,.09), inset 0 1px 0 rgba(255,235,183,.18); }
  .sp-row--rank-1::before { background: linear-gradient(#fff0b2, #ffb23e, #e25bff); box-shadow: 0 0 13px rgba(255,178,62,.65); }
  .sp-row--rank-2 { border-color: rgba(197,211,233,.35); background: linear-gradient(100deg, rgba(197,211,233,.11), rgba(255,255,255,.035)); }
  .sp-row--rank-2::before { background: #b9c7dc; }
  .sp-row--rank-3 { border-color: rgba(201,139,92,.38); background: linear-gradient(100deg, rgba(201,139,92,.11), rgba(255,255,255,.035)); }
  .sp-row--rank-3::before { background: #d28e5e; }
  .sp-movement { display: flex; align-items: center; gap: 5px; min-width: 45px; color: var(--color-muted); font: 600 13px var(--font-mono); }
  .sp-movement-arrow { font: 700 24px var(--font-sans); line-height: .7; }
  .sp-row--up .sp-movement { color: var(--color-success); }
  .sp-row--down .sp-movement { color: var(--color-danger); }
  .sp-movement-blank { width: 18px; height: 2px; background: transparent; }
  .sp-player { display: flex; align-items: center; gap: 13px; min-width: 0; }
  .sp-player strong { overflow: hidden; color: var(--color-text); font-size: 17px; font-weight: 650; text-overflow: ellipsis; white-space: nowrap; }
  .sp-row--rank-1 .sp-player strong { color: #fff0ba; }
  .sp-rank { display: inline-flex; align-items: center; justify-content: center; min-width: 43px; height: 27px; border-radius: 5px; background: rgba(255,255,255,.08); color: var(--color-text-mid); font: 600 11px var(--font-mono); }
  .sp-row--rank-1 .sp-rank { background: rgba(255,178,62,.22); color: #ffe4a0; }
  .sp-row--rank-2 .sp-rank { background: rgba(197,211,233,.14); color: #e0e8f4; }
  .sp-row--rank-3 .sp-rank { background: rgba(201,139,92,.17); color: #edba92; }
  .sp-points { display: flex; align-items: baseline; justify-content: end; gap: 7px; }
  .sp-points strong { color: var(--color-text); font: 700 21px var(--font-mono); letter-spacing: -.06em; }
  .sp-row--rank-1 .sp-points strong { color: #ffe3a0; text-shadow: 0 0 18px rgba(255,178,62,.28); }
  .sp-points span { color: var(--color-muted); font: 9px var(--font-mono); letter-spacing: .1em; }
  .sp-math { display: flex; flex-direction: column; align-items: end; gap: 5px; font: 11px var(--font-mono); }
  .sp-gap { color: #ffd796; }
  .sp-last { color: var(--color-success); }
  .sp-last--negative { color: var(--color-danger); }
  .sp-empty { display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 230px; gap: 9px; color: var(--color-muted); text-align: center; }
  .sp-empty[hidden] { display: none; }
  .sp-empty strong { color: var(--color-text-mid); font-size: 16px; }
  .sp-empty span:last-child { max-width: 390px; font-size: 13px; }
  .sp-empty-icon { color: var(--color-magenta); font-size: 34px; opacity: .7; }
  .sp-footnote { display: flex; gap: 11px; margin: 0 30px 28px; padding: 14px 16px; border: 1px solid rgba(0,212,255,.14); border-radius: 8px; background: rgba(0,212,255,.035); color: var(--color-muted); font-size: 12px; line-height: 1.55; }
  .sp-footnote p { margin: 0; }
  .sp-footnote strong { color: #c7f8ff; }
  .sp-footnote-mark { display: grid; place-items: center; flex: none; width: 19px; height: 19px; border: 1px solid rgba(0,212,255,.55); border-radius: 50%; color: var(--color-cyan); font: 600 11px var(--font-mono); }
  .sp-lower-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 13px; margin-top: 16px; }
  .sp-info-card { display: grid; grid-template-columns: 48px 1fr; gap: 12px; min-height: 190px; padding: 23px; border: 1px solid rgba(255,255,255,.1); border-radius: 13px; background: rgba(17,8,38,.76); }
  .sp-info-card--gold { border-color: rgba(255,178,62,.22); }
  .sp-info-card--cyan { border-color: rgba(0,212,255,.2); }
  .sp-info-number { color: var(--color-warm); font-size: 10px; }
  .sp-info-card--cyan .sp-info-number { color: var(--color-cyan); }
  .sp-info-card h2 { font-size: 25px; }
  .sp-info-card p:last-child { margin: 13px 0 0; color: var(--color-text-mid); font-size: 13px; line-height: 1.6; }
  .sp-footer-line { display: flex; justify-content: space-between; gap: 20px; margin-top: 30px; padding-top: 18px; border-top: 1px solid rgba(255,255,255,.08); color: var(--color-muted); font-size: 9px; }
  .sp-footer-line a { color: var(--color-cyan); transition: color 160ms ease; }
  .sp-footer-line a:hover { color: var(--color-text); }

  @keyframes sp-pulse { 0%, 100% { opacity: .45; transform: scale(.86); } 50% { opacity: 1; transform: scale(1.12); } }
  @keyframes sp-row-in { from { opacity: 0; transform: translateY(9px); } to { opacity: 1; transform: translateY(0); } }
  @media (prefers-reduced-motion: reduce) { .sp-kicker-dot, .sp-status-dot--loading { animation: none; } .sp-row { animation: none; transition: none; } .sp-quiet-button, .sp-refresh-button, .sp-segmented button, .sp-row { transition: none; } }
  @media (max-width: 820px) {
    .sp-shell { width: min(100% - 28px, 700px); padding-top: 68px; }
    .sp-title-row { align-items: start; flex-direction: column; }
    .sp-hero-chip { align-self: start; }
    .sp-stage-topline { align-items: start; flex-direction: column; padding: 23px 20px 18px; }
    .sp-controls { grid-template-columns: 1fr 1fr; padding: 18px 20px 12px; }
    .sp-control--wide { grid-column: 1 / -1; grid-row: 1; }
    .sp-control--view { grid-column: 2; }
    .sp-status-line { padding-left: 20px; padding-right: 20px; }
    .sp-summary { grid-template-columns: 1fr 1fr; }
    .sp-summary-item:nth-child(2) { border-right: 0; }
    .sp-summary-item:nth-child(-n+2) { border-bottom: 1px solid rgba(255,255,255,.075); }
    .sp-board { padding-left: 20px; padding-right: 20px; }
    .sp-board-head { display: none; }
    .sp-row { grid-template-columns: 49px minmax(110px, 1fr) 90px; gap: 9px; padding: 12px 12px; }
    .sp-math { grid-column: 2 / -1; grid-row: 2; flex-direction: row; justify-content: space-between; align-items: center; padding-left: 56px; }
    .sp-points { flex-direction: column; align-items: end; gap: 1px; }
    .sp-points strong { font-size: 17px; }
    .sp-player { gap: 8px; }
    .sp-player strong { font-size: 14px; }
    .sp-rank { min-width: 37px; height: 24px; font-size: 10px; }
    .sp-footnote { margin-left: 20px; margin-right: 20px; }
    .sp-lower-grid { grid-template-columns: 1fr; }
  }
  @media (max-width: 520px) {
    .sp-title-row h1 { font-size: 62px; }
    .sp-intro { font-size: 14px; }
    .sp-stage-actions { width: 100%; }
    .sp-quiet-button, .sp-refresh-button { flex: 1; }
    .sp-controls { grid-template-columns: 1fr; }
    .sp-control--wide, .sp-control--view { grid-column: auto; grid-row: auto; }
    .sp-summary-item { min-height: 62px; padding: 10px 13px; }
    .sp-summary-item strong { font-size: 10px; }
    .sp-row { grid-template-columns: 43px minmax(84px, 1fr) 78px; }
    .sp-movement { gap: 2px; font-size: 11px; }
    .sp-movement-arrow { font-size: 21px; }
    .sp-math { padding-left: 46px; font-size: 10px; }
    .sp-info-card { min-height: 0; padding: 18px; }
    .sp-footer-line { align-items: start; flex-direction: column; }
  }
</style>

<style is:global>
  .standings-page {
    --lb-ink: #070910;
    --lb-panel: rgba(12, 17, 29, .9);
    --lb-line: rgba(255, 255, 255, .1);
    --lb-text: #f3f7ff;
    --lb-muted: #8f9bab;
    --lb-subtle: #5a6575;
    --lb-cyan: #6bdcff;
    --lb-magenta: #d86aff;
    --lb-green: #5cf29a;
    --lb-red: #ff7185;
    --lb-gold: #f2bd57;
    --lb-gold-bright: #ffe4a0;
    --lb-silver: #cad7e8;
    --lb-bronze: #c88b65;
    background: var(--lb-ink);
    isolation: isolate;
  }

  .standings-page :where(button, select, a):focus-visible {
    outline: 2px solid var(--lb-cyan);
    outline-offset: 3px;
  }

  .sp-grid {
    opacity: .28;
    background-image: linear-gradient(rgba(255,255,255,.04) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,.04) 1px, transparent 1px);
    background-size: 64px 64px;
  }

  .sp-shell {
    width: min(1240px, calc(100% - 48px));
    padding: 28px 0 42px;
  }

  .sp-kicker, .sp-section-label, .sp-chip-top, .sp-chip-bottom, .sp-summary-label, .sp-board-head, .sp-footer-line, .sp-info-number {
    letter-spacing: .13em;
  }

  .sp-hero {
    max-width: none;
    padding: 96px 0 58px;
  }

  .sp-title-row h1 {
    font-size: clamp(52px, 8vw, 116px);
    letter-spacing: -.08em;
  }

  .sp-title-gradient {
    background: linear-gradient(105deg, #ffe4a0, #f2bd57 34%, #d86aff 70%, #6bdcff);
  }

  .sp-intro {
    max-width: 650px;
    color: var(--lb-muted);
  }

  .sp-stage {
    border-color: rgba(107,220,255,.22);
    border-radius: 16px;
    background: var(--lb-panel);
    box-shadow: 0 30px 90px rgba(0,0,0,.38), inset 0 1px 0 rgba(255,255,255,.06);
    backdrop-filter: blur(18px);
  }

  .sp-stage-topline {
    padding: 25px 29px 22px;
    background: linear-gradient(110deg, rgba(107,220,255,.055), transparent 35%, rgba(216,106,255,.045));
  }

  .sp-stage h2 {
    margin: 7px 0 0;
    font-size: 27px;
  }

  .sp-board-context {
    display: flex;
    align-items: center;
    gap: 8px;
    max-width: 610px;
    margin-top: 9px;
    overflow: hidden;
    color: var(--lb-muted);
    font: 500 10px var(--font-mono);
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .sp-board-context i {
    color: var(--lb-magenta);
    font-style: normal;
  }

  .sp-stage-actions {
    flex-wrap: wrap;
  }

  .sp-processed-chip {
    padding: 8px 9px;
    border: 1px solid rgba(92,242,154,.22);
    border-radius: 5px;
    color: var(--lb-green);
    background: rgba(92,242,154,.035);
    font: 600 8px var(--font-mono);
    letter-spacing: .08em;
    white-space: nowrap;
  }

  .sp-quiet-button, .sp-refresh-button, .sp-segmented button {
    border-radius: 5px;
    font-size: 9px;
  }

  .sp-controls {
    padding: 18px 29px 16px;
  }

  .sp-control select {
    border-radius: 5px;
    background: #0a111d;
  }

  .sp-status-line {
    min-height: 41px;
    padding: 0 29px;
    border-bottom: 1px solid rgba(255,255,255,.07);
    font: 500 10px var(--font-mono);
  }

  .sp-status-delay {
    margin-left: auto;
    color: var(--lb-subtle);
    font: 600 8px var(--font-mono);
    letter-spacing: .1em;
  }

  .sp-summary {
    grid-template-columns: 1.2fr .8fr 1.1fr .8fr 1.2fr;
  }

  .sp-summary-item {
    min-height: 74px;
    padding: 13px 16px;
  }

  .sp-summary-item strong {
    font-size: 11px;
  }

  .sp-board {
    padding: 0 29px 25px;
  }

  .sp-board-head, .sp-row {
    grid-template-columns: 72px minmax(200px, 1.35fr) 104px 72px 130px minmax(160px, .9fr);
    gap: 13px;
  }

  .sp-board-head {
    padding: 19px 16px 10px;
    font-size: 8px;
  }

  .sp-board-head span:nth-child(n+3) {
    text-align: right;
  }

  .sp-row {
    min-height: 80px;
    padding: 11px 16px;
    border-color: var(--lb-line);
    border-radius: 8px;
    background: rgba(255,255,255,.035);
    animation: sp-row-in 480ms cubic-bezier(.2,.8,.2,1) both;
  }

  .sp-row:hover {
    border-color: rgba(255,255,255,.24);
    background: rgba(255,255,255,.07);
  }

  .sp-row--rank-1 {
    border-color: rgba(242,189,87,.58);
    background: linear-gradient(100deg, rgba(242,189,87,.21), rgba(242,189,87,.055) 47%, rgba(255,255,255,.035));
    box-shadow: 0 0 38px rgba(242,189,87,.11), inset 0 1px 0 rgba(255,236,181,.17);
  }

  .sp-row--rank-2 {
    border-color: rgba(202,215,232,.42);
    background: linear-gradient(100deg, rgba(202,215,232,.14), rgba(255,255,255,.035));
    box-shadow: 0 0 27px rgba(202,215,232,.055);
  }

  .sp-row--rank-3 {
    border-color: rgba(200,139,101,.42);
    background: linear-gradient(100deg, rgba(200,139,101,.14), rgba(255,255,255,.035));
    box-shadow: 0 0 27px rgba(200,139,101,.055);
  }

  .sp-row--rank-1::before { background: linear-gradient(#fff1b8, var(--lb-gold), var(--lb-magenta)); box-shadow: 0 0 14px rgba(242,189,87,.72); }
  .sp-row--rank-2::before { background: var(--lb-silver); }
  .sp-row--rank-3::before { background: var(--lb-bronze); }

  .sp-movement {
    color: var(--lb-subtle);
    font: 600 12px var(--font-mono);
  }

  .sp-movement-arrow {
    font-size: 23px;
  }

  .sp-row--up .sp-movement { color: var(--lb-green); }
  .sp-row--down .sp-movement { color: var(--lb-red); }

  .sp-player {
    gap: 12px;
  }

  .sp-player-copy {
    display: grid;
    min-width: 0;
    gap: 4px;
    padding: 5px 10px 5px 11px;
    border-left: 2px solid rgba(107,220,255,.42);
    border-radius: 4px;
    background: linear-gradient(90deg, rgba(107,220,255,.1), rgba(107,220,255,0) 88%);
    box-shadow: 0 0 20px rgba(107,220,255,.08);
  }

  .standings-page .sp-player strong {
    display: block;
    font-size: 15px;
    font-weight: 750;
    letter-spacing: -.02em;
    color: #eaf7ff;
    text-shadow: 0 0 16px rgba(107,220,255,.34);
  }

  .sp-player-copy small {
    overflow: hidden;
    color: var(--lb-muted);
    font: 500 9px var(--font-mono);
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .sp-row--rank-1 .sp-player-copy {
    border-left-color: var(--lb-gold);
    background: linear-gradient(90deg, rgba(242,189,87,.16), rgba(242,189,87,0) 88%);
    box-shadow: 0 0 22px rgba(242,189,87,.12);
  }

  .standings-page .sp-row--rank-1 .sp-player-copy strong {
    color: #fff0bf;
    text-shadow: 0 0 20px rgba(242,189,87,.48);
  }

  .sp-row--rank-2 .sp-player-copy {
    border-left-color: var(--lb-silver);
    background: linear-gradient(90deg, rgba(202,215,232,.12), rgba(202,215,232,0) 88%);
  }

  .standings-page .sp-row--rank-2 .sp-player-copy strong {
    color: #f1f6ff;
    text-shadow: 0 0 17px rgba(202,215,232,.36);
  }

  .sp-row--rank-3 .sp-player-copy {
    border-left-color: var(--lb-bronze);
    background: linear-gradient(90deg, rgba(200,139,101,.13), rgba(200,139,101,0) 88%);
  }

  .standings-page .sp-row--rank-3 .sp-player-copy strong {
    color: #ffe0c9;
    text-shadow: 0 0 17px rgba(200,139,101,.38);
  }

  .sp-row--standard .sp-player-copy {
    border-left-color: rgba(107,220,255,.56);
    background: linear-gradient(90deg, rgba(73,160,224,.12), rgba(73,160,224,0) 88%);
  }

  .standings-page .sp-row--standard .sp-player-copy strong {
    color: #dff3ff;
    text-shadow: 0 0 17px rgba(83,183,255,.36);
  }

  .sp-rank {
    width: 42px;
    min-width: 42px;
    height: 29px;
    border: 1px solid rgba(255,255,255,.1);
    color: var(--lb-muted);
  }

  .sp-row--rank-1 .sp-rank { border-color: rgba(242,189,87,.35); background: rgba(242,189,87,.2); color: var(--lb-gold-bright); }
  .sp-row--rank-2 .sp-rank { border: 1px solid rgba(202,215,232,.23); background: rgba(202,215,232,.14); color: #e4ecf6; }
  .sp-row--rank-3 .sp-rank { border: 1px solid rgba(200,139,101,.25); background: rgba(200,139,101,.16); color: #f1b893; }

  .sp-trophy {
    width: 18px;
    height: 18px;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.65;
  }

  .sp-row--rank-1 .sp-rank,
  .sp-row--rank-1 .sp-points {
    position: relative;
    overflow: visible;
  }

  .sp-row--rank-1 .sp-rank::before,
  .sp-row--rank-1 .sp-rank::after,
  .sp-row--rank-1 .sp-points::before,
  .sp-row--rank-1 .sp-points::after {
    position: absolute;
    z-index: 2;
    color: #fff9d5;
    content: '✦';
    font: 700 11px/1 var(--font-sans);
    opacity: 0;
    pointer-events: none;
    text-shadow: 0 0 7px #fff, 0 0 16px rgba(242,189,87,.95), 0 0 28px rgba(255,183,42,.68);
    animation: sp-trophy-sparkle 2.9s ease-in-out infinite;
  }

  .sp-row--rank-1 .sp-rank::before { top: -10px; left: -7px; }
  .sp-row--rank-1 .sp-rank::after { right: -8px; bottom: -9px; animation-delay: 1.15s; }
  .sp-row--rank-1 .sp-points::before { top: -8px; left: -11px; animation-delay: .55s; }
  .sp-row--rank-1 .sp-points::after { top: -15px; right: -4px; animation-delay: 1.75s; }

  .sp-rank-roman {
    font: 800 13px var(--font-mono);
    letter-spacing: -.1em;
  }

  .sp-rank-number {
    font: 600 11px var(--font-mono);
  }

  .sp-points, .sp-wins, .sp-prize {
    display: flex;
    flex-direction: column;
    align-items: end;
    gap: 4px;
    min-width: 0;
  }

  .sp-points strong {
    color: var(--lb-text);
    font: 700 20px var(--font-mono);
    letter-spacing: -.07em;
  }

  .sp-row--rank-1 .sp-points strong {
    position: relative;
    z-index: 1;
    color: #ffe7a8;
    text-shadow: 0 0 22px rgba(242,189,87,.72), 0 0 42px rgba(255,215,109,.34);
    filter: drop-shadow(0 0 7px rgba(255,190,62,.58));
  }

  .sp-points span, .sp-wins span, .sp-prize span {
    color: var(--lb-subtle);
    font: 600 8px var(--font-mono);
    letter-spacing: .1em;
  }

  .sp-wins strong {
    color: var(--lb-text);
    font: 600 15px var(--font-mono);
  }

  .sp-prize {
    position: relative;
  }

  .sp-prize strong {
    position: relative;
    z-index: 1;
    color: var(--lb-green);
    font: 700 15px var(--font-mono);
    letter-spacing: -.03em;
  }

  .sp-prize--tier-1 strong { color: var(--lb-gold-bright); text-shadow: 0 0 16px rgba(242,189,87,.5); }
  .sp-prize--tier-2 strong { color: var(--lb-silver); text-shadow: 0 0 14px rgba(202,215,232,.34); }
  .sp-prize--tier-3 strong { color: #e6a57d; text-shadow: 0 0 14px rgba(200,139,101,.34); }
  .sp-prize--tier-1::before, .sp-prize--tier-2::before, .sp-prize--tier-3::before { position: absolute; top: -12px; right: -12px; width: 54px; height: 54px; border-radius: 50%; content: ''; opacity: .24; pointer-events: none; }
  .sp-prize--tier-1::before { background: radial-gradient(circle, var(--lb-gold) 0, transparent 68%); }
  .sp-prize--tier-2::before { background: radial-gradient(circle, var(--lb-silver) 0, transparent 68%); }
  .sp-prize--tier-3::before { background: radial-gradient(circle, var(--lb-bronze) 0, transparent 68%); }

  .sp-math {
    display: flex;
    flex-direction: column;
    align-items: end;
    gap: 6px;
    min-width: 0;
  }

  .sp-gap {
    overflow: hidden;
    color: #f7ce74;
    font: 600 11px var(--font-mono);
    text-overflow: ellipsis;
    white-space: nowrap;
    cursor: help;
    text-decoration: underline dotted rgba(247,206,116,.45);
    text-underline-offset: 3px;
  }

  .sp-gap:hover { text-shadow: 0 0 14px rgba(247,206,116,.6); }

  .sp-last {
    color: var(--lb-green);
    font: 500 10px var(--font-mono);
    white-space: nowrap;
  }

  .sp-last--negative { color: var(--lb-red); }
  .sp-points-flash { animation: sp-point-up 720ms ease both; }
  .sp-points-flash-red { animation: sp-point-down 720ms ease both; }
  .sp-row--rank-1 .sp-points strong.sp-points-flash {
    animation: sp-point-up 720ms ease both;
  }
  .sp-row--rank-1 .sp-points strong.sp-points-flash-red {
    animation: sp-point-down 720ms ease both;
  }

  .sp-overlay-tools {
    display: none;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    min-height: 47px;
    padding: 9px 13px;
    border-bottom: 1px solid var(--lb-line);
    background: rgba(4,7,13,.6);
  }

  .sp-overlay-brand {
    display: flex;
    align-items: center;
    gap: 7px;
    color: var(--lb-text);
    font: 700 10px var(--font-mono);
    letter-spacing: .12em;
  }

  .sp-overlay-brand i { color: var(--lb-gold); font-style: normal; }
  .sp-overlay-mark { display: grid; place-items: center; width: 22px; height: 22px; border: 1px solid rgba(107,220,255,.58); border-radius: 5px; color: white; background: linear-gradient(145deg, rgba(216,106,255,.8), rgba(107,220,255,.32)); font: 800 11px var(--font-sans); }
  .sp-overlay-tool-actions { display: flex; align-items: center; gap: 9px; }
  .sp-segmented--overlay { height: 32px; }
  .sp-segmented--overlay button { min-width: 34px; padding: 0 9px; }
  .sp-overlay-collapse, .sp-overlay-full { border: 1px solid var(--lb-line); border-radius: 5px; cursor: pointer; padding: 8px 9px; color: var(--lb-muted); background: rgba(255,255,255,.035); font: 600 9px var(--font-mono); text-decoration: none; }
  .sp-overlay-collapse { display: inline-flex; align-items: center; gap: 6px; }
  .sp-overlay-collapse:hover, .sp-overlay-full:hover { border-color: var(--lb-cyan); color: var(--lb-text); }
  .sp-overlay-collapse > span:last-child { color: var(--lb-cyan); font-size: 15px; line-height: .6; }
  .sp-overlay-full { color: var(--lb-cyan); }

  .sp-overlay-mode {
    min-height: 0;
    overflow: visible;
    background: transparent;
  }

  .sp-overlay-mode .sp-grid, .sp-overlay-mode .sp-hero, .sp-overlay-mode .sp-controls, .sp-overlay-mode .sp-status-line, .sp-overlay-mode .sp-summary, .sp-overlay-mode .sp-footnote, .sp-overlay-mode .sp-lower-grid, .sp-overlay-mode .sp-footer-line { display: none; }
  .sp-overlay-mode .sp-shell { width: min(100%, 560px); margin: 0; padding: 10px; }
  .sp-overlay-mode .sp-stage { border-color: rgba(107,220,255,.3); border-radius: 10px; background: rgba(7,10,17,.92); box-shadow: 0 16px 40px rgba(0,0,0,.45); }
  .sp-overlay-mode .sp-stage-topline { align-items: center; min-height: 40px; padding: 10px 12px; }
  .sp-overlay-mode .sp-stage-topline > div:first-child { display: flex; align-items: center; gap: 8px; min-width: 0; }
  .sp-overlay-mode .sp-stage-topline .sp-section-label { display: none; }
  .sp-overlay-mode .sp-stage h2 { margin: 0; font: 700 11px var(--font-mono); letter-spacing: .12em; text-transform: uppercase; white-space: nowrap; }
  .sp-overlay-mode .sp-stage h2::after { color: var(--lb-gold); content: ' • LIVE BOARD'; }
  .sp-overlay-mode .sp-board-context { margin: 0; font-size: 8px; }
  .sp-overlay-mode .sp-stage-actions { display: none; }
  .sp-overlay-mode .sp-overlay-tools { display: flex; }
  .sp-overlay-mode .sp-board { padding: 0 10px 10px; }
  .sp-overlay-mode .sp-board-head { display: none; }
  .sp-overlay-mode .sp-row { grid-template-columns: 38px minmax(0, 1fr) 75px; grid-template-areas: 'move player points' 'move math math'; gap: 0 8px; min-height: 64px; padding: 8px 9px; }
  .sp-overlay-mode .sp-movement { grid-area: move; align-self: center; }
  .sp-overlay-mode .sp-player { grid-area: player; }
  .sp-overlay-mode .sp-points { grid-area: points; }
  .sp-overlay-mode .sp-wins, .sp-overlay-mode .sp-prize { display: none; }
  .sp-overlay-mode .sp-math { grid-area: math; flex-direction: row; align-items: center; justify-content: space-between; padding-top: 4px; }
  .sp-overlay-mode .sp-rank { width: 30px; min-width: 30px; height: 24px; }
  .sp-overlay-mode .sp-trophy { width: 15px; height: 15px; }
  .sp-overlay-mode .sp-player { gap: 7px; }
  .sp-overlay-mode .sp-player strong { font-size: 12px; }
  .sp-overlay-mode .sp-player-copy small { font-size: 8px; }
  .sp-overlay-mode .sp-points strong { font-size: 16px; }
  .sp-overlay-mode .sp-points span { font-size: 7px; }
  .sp-overlay-mode .sp-gap, .sp-overlay-mode .sp-last { font-size: 8px; }
  .sp-overlay-mode.sp-overlay-collapsed .sp-board { display: none; }
  .sp-overlay-mode.sp-overlay-collapsed .sp-stage-topline { border-bottom: 0; }
  .sp-overlay-mode.sp-overlay-collapsed .sp-overlay-collapse > span:last-child { transform: rotate(180deg); }

  @keyframes sp-point-up { 0% { color: var(--lb-text); transform: scale(1); } 35% { color: var(--lb-green); transform: scale(1.15); text-shadow: 0 0 25px rgba(92,242,154,.8); } 100% { color: var(--lb-green); transform: scale(1); } }
  @keyframes sp-point-down { 0% { color: var(--lb-text); transform: scale(1); } 35% { color: var(--lb-red); transform: scale(1.15); text-shadow: 0 0 25px rgba(255,113,133,.8); } 100% { color: var(--lb-red); transform: scale(1); } }
  @keyframes sp-trophy-sparkle {
    0%, 100% { opacity: 0; transform: scale(.45) rotate(0deg); }
    22% { opacity: 1; transform: scale(1) rotate(18deg); }
    44% { opacity: .35; transform: scale(.72) rotate(42deg); }
    60% { opacity: 0; transform: scale(.35) rotate(60deg); }
  }

  @media (prefers-reduced-motion: reduce) {
    .sp-kicker-dot, .sp-status-dot--loading, .sp-row, .sp-points-flash, .sp-points-flash-red, .sp-row--rank-1 .sp-points strong, .sp-row--rank-1 .sp-rank::before, .sp-row--rank-1 .sp-rank::after, .sp-row--rank-1 .sp-points::before, .sp-row--rank-1 .sp-points::after { animation: none; }
    .sp-row--rank-1 .sp-rank::before, .sp-row--rank-1 .sp-rank::after, .sp-row--rank-1 .sp-points::before, .sp-row--rank-1 .sp-points::after { opacity: .72; transform: scale(.72); }
    .sp-quiet-button, .sp-refresh-button, .sp-segmented button, .sp-row { transition: none; }
  }

  @media (max-width: 980px) {
    .sp-summary { grid-template-columns: repeat(3, 1fr); }
    .sp-summary-item:nth-child(3) { border-right: 0; }
    .sp-summary-item:nth-child(-n+3) { border-bottom: 1px solid rgba(255,255,255,.075); }
    .sp-summary-item--source { grid-column: 1 / -1; }
  }

  @media (max-width: 820px) {
    .sp-shell { width: min(100% - 28px, 700px); }
    .sp-stage-topline { align-items: start; flex-direction: column; padding: 23px 20px 18px; }
    .sp-stage-actions { width: 100%; justify-content: start; }
    .sp-controls { grid-template-columns: 1fr 1fr; padding: 17px 20px 14px; }
    .sp-control--wide { grid-column: 1 / -1; grid-row: 1; }
    .sp-control--view { grid-column: 2; grid-row: 2; }
    .sp-status-line { padding-left: 20px; padding-right: 20px; }
    .sp-board { padding-left: 20px; padding-right: 20px; }
    .sp-board-head { display: none; }
    .sp-row { grid-template-columns: 48px minmax(110px, 1fr) 82px; grid-template-areas: 'move player points' 'move math math'; gap: 0 9px; min-height: 83px; padding: 10px 12px; }
    .sp-movement { grid-area: move; }
    .sp-player { grid-area: player; }
    .sp-points { grid-area: points; }
    .sp-wins, .sp-prize { display: none; }
    .sp-math { grid-area: math; flex-direction: row; justify-content: space-between; align-items: center; padding-top: 6px; }
    .sp-player strong { font-size: 14px; }
    .sp-points strong { font-size: 17px; }
    .sp-footnote { margin-left: 20px; margin-right: 20px; }
  }

  @media (max-width: 520px) {
    .sp-shell { width: min(100% - 20px, 500px); padding-top: 18px; }
    .sp-title-row h1 { font-size: 62px; }
    .sp-intro { font-size: 14px; }
    .sp-stage-actions { display: grid; grid-template-columns: 1fr 1fr 36px; }
    .sp-processed-chip { grid-column: 1 / -1; justify-self: start; }
    #sp-share, #sp-copy-overlay { grid-row: 2; }
    #sp-open-overlay { grid-row: 2; grid-column: 2; }
    #sp-refresh { grid-row: 2; grid-column: 3; }
    .sp-controls { grid-template-columns: 1fr; }
    .sp-control--wide, .sp-control--view { grid-column: auto; grid-row: auto; }
    .sp-field--view .sp-segmented { width: 100%; }
    .sp-summary-item { min-height: 63px; padding: 11px 12px; }
    .sp-summary-item strong { font-size: 10px; }
    .sp-summary-item--source { grid-column: 1 / -1; }
    .sp-row { grid-template-columns: 42px minmax(84px, 1fr) 73px; padding-left: 9px; padding-right: 9px; }
    .sp-movement { gap: 2px; font-size: 10px; }
    .sp-movement-arrow { font-size: 20px; }
    .sp-rank { width: 34px; min-width: 34px; }
    .sp-player { gap: 6px; }
    .sp-player strong { font-size: 12px; }
    .sp-player-copy small { font-size: 8px; }
    .sp-gap, .sp-last { font-size: 9px; }
    .sp-info-card { min-height: 0; padding: 18px; }
    .sp-footer-line { align-items: start; flex-direction: column; }
    .sp-overlay-mode .sp-shell { width: 100%; padding: 6px; }
    .sp-overlay-mode .sp-stage-topline { padding: 9px; }
    .sp-overlay-mode .sp-overlay-tools { padding: 7px 9px; }
    .sp-overlay-brand { font-size: 8px; }
    .sp-overlay-mode .sp-overlay-full { display: none; }
  }
</style>
